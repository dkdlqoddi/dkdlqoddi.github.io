# 팀 세미나 진행 스크립트 — DX vs AX (1부) + AI Agent Skills Philosophy (2부)

- 대상 자료: https://dkdlqoddi.github.io/slides/dx-vs-ax-automation/ (14장) → https://dkdlqoddi.github.io/slides/ai-agent-skills-philosophy/ (27장)
- 기준 시간: **본편 45분 + Q&A 15분** (압축 경로는 맨 아래: 약 37분 / 약 33분)
- 표기: `[→ n]` = 이 장에서 화살표 키를 누르는 횟수. 누를 때마다 그림 조각·터미널 줄이 하나씩 나타납니다.

---

## 0. 발표 전 체크리스트 (5분 전)

- [ ] 크롬으로 1부 주소를 열고 `F` 전체화면. (오프라인 대비: 저장소 폴더에서 `python3 -m http.server 8000` → http://localhost:8000/slides/dx-vs-ax-automation/)
- [ ] OS의 "동작 줄이기(reduce motion)" 설정이 **꺼져** 있는지 확인 — 켜져 있으면 흐르는 점·행진 점선이 정지된 채 나옵니다(깨진 게 아니라 접근성 동작).
- [ ] 키 조작 리허설: `→` 다음, `←` 이전, `Esc` 조망(전체 지도), `숫자+Enter` 장 점프, `B` 검은 화면(휴지).
- [ ] 1부 마지막 장에 **2부로 가는 링크**가 있으므로 탭을 미리 열어 둘 필요는 없습니다.
- [ ] 유인물이 필요하면: **덱마다 따로** 주소 뒤에 `?print-pdf`를 붙여 연 뒤 브라우저 인쇄 → PDF. 1부 14쪽 + 2부 27쪽 = 합계 41쪽(내용이 긴 장이 2쪽으로 나뉘면 조금 늘 수 있음), fragment는 최종 상태로 나옵니다.

---

## 1부 — DX vs AX (약 15분)

### 1. 표지 `[→ 0]` · 0:30
> "오늘 세미나는 두 부로 갑니다. 여러분 모두 opencode를 기본적으로 써 보셨으니, 오늘은 '어떻게 쓰나'가 아니라 **'왜 이렇게 동작하고, 어디까지 맡길 수 있나'** 를 그림으로 봅니다. 1부는 LLM 자동화가 무엇을 바꾸는지, 2부는 그 힘을 통제하는 장치 — 하네스입니다."

### 2. 오늘의 여정 `[→ 0]` · 1:00
> "지도 먼저 보겠습니다. 1부에서 파이프라인의 진화, 규칙이 못 덮는 긴 꼬리, 사내 LLM 연결 4가지 방법을 봅니다. 2부는 하네스 — 결과의 폭을 조이는 원리와, 그걸 구현한 스킬 다섯·제품 다섯입니다. **1부의 마지막 질문이 2부의 첫 그림이 됩니다.** 이 문장 기억해 두세요."

### 3. DX와 AX `[→ 1]` · 1:00
> "용어부터 정리합니다. DX, 디지털 전환은 규칙 기반 자동화입니다 — 결정론적이고, 정형 데이터를 다룹니다." `[→]` "AX, AI 전환은 LLM의 추론이 그 자리를 대신합니다 — 적응형이고, 비정형 자연어까지 다룹니다. 오늘 이야기는 이 화살표 하나, '진화'가 실제로 무엇을 바꾸는가입니다."

### 4. DX 자동화 파이프라인 `[→ 1]` · 1:30
> "전형적인 규칙 기반 파이프라인입니다. 수집 → 규칙 분기 → 고정 처리 → 출력. 지금 흰 점이 흐르고 있죠 — 정형 입력은 잘 흘러갑니다." `[→]` "문제는 이겁니다. **규칙에 없는 입력이 오면? 실패합니다.** 새 형식, 오탈자, 예외 — 올 때마다 규칙을 하나씩 추가해 온 게 우리 모두의 경험이죠."

### 5. 같은 파이프라인, AX로 진화 `[→ 2]` · 1:30
> "같은 파이프라인에서 핵심 상자 두 개를 바꿉니다 — 규칙 분기가 **LLM 판단**으로, 고정 처리가 **동적 처리**로. 그러면 양끝 상자의 성격까지 따라 바뀝니다: 입력은 비정형까지, 출력은 유연한 형식으로." `[→]` "그러면 따라오는 게 생깁니다 — 자연어 이해, 맥락 기억, 자기 치유." `[→]` "그리고 이 점선 루프. **결과를 보고 판단을 보정하는 피드백**이 파이프라인 안에 생깁니다. 규칙에는 없던 것입니다."

### 6. 효과 — 규칙이 못 덮는 긴 꼬리 `[→ 2]` · 2:00
> "1부에서 제일 중요한 그림입니다. 가로축은 입력의 다양성, 세로축은 빈도. 자주 오는 정형 입력은 왼쪽 머리에 몰려 있고, 규칙 기반은 딱 여기까지 덮습니다. 한계선 오른쪽 꼬리는 전부 실패 표시죠." `[→]` "LLM 기반은 **꼬리 끝까지** 덮습니다. 실패가 처리로 바뀝니다." `[→]` "핵심은 이 문장입니다 — 예외마다 규칙을 하나씩 늘리는 대신, **판단 하나가 꼬리 전체를 덮는다.**"

### 7. 사내 LLM에 연결하는 4가지 방법 `[→ 3]` · 1:30
> "그럼 어떻게 연결하느냐. 추상화가 낮은 것부터 계단으로 네 가지입니다. 첫째, HTTP 직접 호출." `[→]` "둘째, OpenAI SDK — 얇은 래퍼." `[→]` "셋째, LangChain — 프레임워크." `[→]` "넷째, opencode — 여기만 성격이 다릅니다. 아래 문장이 오늘의 분기점입니다: **①→③은 LLM을 "호출"하고, ④는 일을 "위임"합니다.**"

### 8. 방법 1 — HTTP 직접 호출 `[→ 0]` · 1:00
> "표준 chat/completions 규격에 POST 한 번입니다. requests 한 줄이면 되고, 폐쇄망에서는 표준 라이브러리 urllib만으로도 됩니다. 단발 호출·최소 설치면 이걸로 충분합니다."

### 9. 방법 2 — OpenAI SDK `[→ 0]` · 1:00
> "같은 API를 SDK로. base_url만 사내 주소로 바꾸면 끝입니다. 타입과 스트리밍이 필요할 때, 호환 서버의 사실상 표준입니다."

### 10. 방법 3 — LangChain `[→ 0]` · 1:00
> "프롬프트 템플릿과 모델을 체인으로 조립합니다. 다단계 작업을 코드로 엮을 때 씁니다. 여기까지 셋은 전부 '호출' — 판단은 여전히 우리 코드가 합니다."

### 11. 방법 4 — opencode, 에이전트에게 위임 `[→ 0]` · 1:00
> "넷째가 오늘의 주인공입니다. subprocess로 opencode를 부르면, 에이전트가 **판단하고, 도구를 실행하고, 재시도까지** 알아서 합니다. 점선 테두리가 돌고 있는 상자 — 자율 영역이라는 뜻입니다. 호출이 아니라 **위임**입니다."

### 12. 4가지 방법 비교 `[→ 4]` · 1:00
> `[→]×4로 한 줄씩 열며` "정리하면 — 단발 호출은 HTTP, 타입·스트리밍은 SDK, 다단계 조립은 LangChain, 그리고 도구 실행까지 통째로 맡길 거면 opencode. 상황별 선택이지 우열이 아닙니다."

### 13. 위임의 대가 — 폭이 벌어진다 `[→ 1]` · 1:00
> "그런데 위임에는 대가가 있습니다. 같은 요청을 통째로 맡기면, 결과가 이렇게 **부채꼴로 퍼집니다.** 잘된 결과도, 망한 결과도 이 안에 있습니다." `[→]` "이 폭을 조이는 장치가 **하네스**고, 그게 2부 전체입니다."

### 14. 1부 끝 `[→ 0]` · 0:30
> "여기서 2부로 바로 넘어갑니다." (화면의 **2부 링크 클릭**)

---

## 2부 — AI Agent Skills Philosophy (약 28분)

### 1. 표지 `[→ 0]` · 0:30
> "2부입니다. 하네스 — 결과의 폭을 조이는 장치. 도구 열 개를 보는데, 하나마다 **구조 한 장 + 실제 동작 장면 한 장**으로 봅니다."

### 2. 같은 요청, 벌어지는 결과 `[→ 0]` · 1:30
> "1부 마지막 그림 그대로입니다. 점 하나가 같은 요청의 1회 실행 결과입니다. 열두 번 돌리면 이렇게 흩어집니다 — 위쪽은 잘된 결과, 아래쪽은 망한 결과. **모델이 나빠서가 아니라, 이게 확률적 생성의 본성입니다.**"

### 3. 하네스가 폭을 조인다 `[→ 0]` · 2:00
> (장을 넘기는 순간 부채꼴이 띠로 **조여지는 모핑**이 재생됩니다 — 잠깐 멈춰서 보여 주세요.)
> "하네스는 네 층입니다. ① 지시문 — 점선 쐐기, 부드럽게 밉니다. ③ 작업 절차, 스킬 — 역시 연성. ② 도구 제한 — 실선 막대, 물리적으로 자릅니다. ④ 검증 루프 — 어긋난 결과를 **반려해서 재실행**시키는 문. 지금 빨간 점선이 흐르는 게 그 반려 경로입니다. 결과를 하나로 고정하는 게 아니라, 원하는 결과가 **더 자주** 나오게 조이는 겁니다. 탐색이 필요할 땐 일부러 넓게 두고요."

### 4. 오늘의 지도 — 조여진 띠 아래에서 `[→ 2]` · 1:30
> (띠가 위로 올라가 리본이 되는 모핑) "이 띠 아래에서 오늘 볼 열 개입니다." `[→]` "왼쪽 다섯은 **스킬 — 특정 층만 조입니다.** 이름 옆 번호가 방금 그 층입니다(둘을 함께 조이는 스킬도 있습니다)." `[→]` "오른쪽 다섯은 **제품 — 하네스를 통째로** 담았습니다. 스킬 다섯부터 갑니다."

### 5. ponytail `[→ 0]` · 0:45
> "첫째, ponytail — 게으른 시니어 개발자를 주입하는 스킬입니다. 코드를 쓰기 전에 사다리를 태웁니다: 정말 필요한가, 이미 있는 걸로 되나, 한 줄로 되나. 실제 사다리는 아래 캡션처럼 7단입니다. 탈락하면? 만들지 않습니다."

### 6. ponytail — 동작 보기 `[→ 7]` · 1:15
> 도입: "말로 하면 저렇고, 실제로 돌리면 이렇게 됩니다." — **한 줄 공개할 때마다 화면 그대로 소리 내어 읽기.**
> 촌평: "결과물이 코드 1줄 + 한계를 적은 주석 1줄. 안 쓴 코드가 이 스킬의 성과입니다."

### 7. grill-me `[→ 0]` · 0:45
> "둘째, grill-me — Matt Pocock의 커뮤니티 스킬입니다. 계획을 실행하기 전에 '갈굼', 집요한 인터뷰를 겁니다. 특징은 질문을 **라운드로 묶어 던지고, 질문마다 자기 권장 답을 붙인다**는 것. 파일을 안 남기는 무상태 도구라, 산출물은 정제된 합의뿐입니다."

### 8. grill-me — 동작 보기 `[→ 8]` · 1:15
> 도입: "설계안 하나가 한 라운드에 어떻게 다듬어지는지 보시죠." — 줄 읽기.
> 촌평: "질문 세 개에 권장 답 세 개 — 답하는 쪽 부담을 줄이면서 모호함만 정확히 걷어냅니다."

### 9. superpowers `[→ 0]` · 0:45
> "셋째, superpowers — Jesse Vincent와 Prime Radiant의 프레임워크입니다. 구상 → 워크트리 격리 → 계획 → 실행. 중요한 건 TDD와 리뷰가 마지막 단계가 아니라 **실행 루프 안에서 태스크마다 반복**된다는 점입니다. 공식 문구가 이겁니다: Mandatory workflows, not suggestions."

### 10. superpowers — 동작 보기 `[→ 8]` · 1:15
> 도입: "기능 요청 하나가 스킬 사슬을 어떻게 타는지 봅니다." — 줄 읽기.
> 촌평: "스킬이 스킬을 부릅니다. 절차가 사람 기억이 아니라 시스템에 있다는 게 핵심입니다."

### 11. blindspot-flow `[→ 0]` · 0:45
> "넷째, blindspot-flow — 우리 저장소에서 실제로 쓰는 스킬입니다. 코드 전에 맹점을 캐는 인터뷰, 미지 영역 문서, 명세, 그리고 마지막에 **보고서와 퀴즈**. 퀴즈를 통과하기 전에는 머지 금지 — ③절차와 ④검증을 함께 조입니다."

### 12. blindspot-flow — 동작 보기 `[→ 8]` · 1:15
> 도입: "기능 하나의 수명주기를 봅니다." — 줄 읽기.
> 촌평(마지막 줄 공개 후): "여러분이 지금 보는 **이 발표 자료가 방금 이 파이프라인을 통과해서 만들어졌습니다.** 퀴즈까지 통과하고 왔습니다."

### 13. fablize `[→ 0]` · 0:45
> "다섯째, fablize — fivetaku의 공개 플러그인입니다. 'Fable처럼 행동하는 Opus' — 완료·증거·검증을 절차로 강제합니다. 그림의 게이트가 상징이지만 실제로는 4중입니다: 태스크별 라우터, 실행 관찰, 검증 게이트, 조사 프로토콜."

### 14. fablize — 동작 보기 `[→ 7]` · 1:15
> 도입: "'다 했습니다'라는 말의 운명을 보시죠." — 줄 읽기.
> 촌평: "됐다는 말이 아니라, 실행을 관찰한 증거가 일을 끝냅니다. ④검증 루프의 순수한 구현입니다."

### 15. 제품 다섯 — 자율성 스펙트럼 `[→ 5]` · 1:00
> "이제 제품 다섯. 축은 자율성입니다." `[→]×5로 하나씩` "Aider, Copilot Cloud Agent, LangChain Agent, oh-my-openagent, Devin. **오른쪽으로 갈수록 위임 범위가 넓고 — 1부의 결론대로 — 더 무거운 하네스가 함께 갑니다.** 이 순서대로 봅니다."

### 16. Aider `[→ 0]` · 0:45
> "Aider — 터미널 페어 프로그래밍의 원형입니다. 수정마다 자동 커밋이 기본값이고, --auto-test를 켜면 테스트 실패 출력이 그대로 다음 입력이 됩니다. architect/editor 두 모델 분업 모드도 있습니다."

### 17. Aider — 동작 보기 `[→ 8]` · 1:15
> 도입: "버그 수정 루프 한 바퀴입니다." — 줄 읽기.
> 촌평: "터미널 출력 전체가 피드백 — 루프가 곧 하네스입니다."

### 18. Copilot Cloud Agent `[→ 0]` · 1:00
> "다음, GitHub의 Copilot Cloud Agent. 혹시 Copilot Workspace를 기억하시면 — **그 제품은 아카이브됐고**, 명세·계획 중심 철학이 이쪽으로 계승됐습니다. 이슈·패널·PR 코멘트로 위임하면 GitHub Actions 임시 환경에서 리서치 → 계획 → 코드를 수행하고, 결과는 브랜치 커밋과 세션 로그로 남습니다. **PR은 사람이 diff를 확인한 뒤 결정합니다.**"

### 19. Copilot Cloud Agent — 동작 보기 `[→ 7]` · 1:00
> 도입: "이슈 하나를 맡기면 이렇게 흘러갑니다." — 줄 읽기.
> 촌평: "개발자의 개입 지점이 코드 안에서 이슈와 PR의 경계로 이동합니다."

### 20. LangChain Agent `[→ 0]` · 0:45
> "LangChain 에이전트. 지금 표준은 LangGraph 런타임 위 create_agent이고, 구조는 ReAct 루프입니다 — 별도의 라우터가 있는 게 아니라 **모델이 매 턴 도구를 고릅니다.** SQL, 검색, 사내 API를 도구로 등록하는 식입니다."

### 21. LangChain Agent — 동작 보기 `[→ 7]` · 1:00
> 도입: "매출 질문 하나의 ReAct 추적 로그입니다." — 줄 읽기.
> 촌평: "생각 → 행동 → 관찰 로그가 그대로 실행 기록 — 루프가 곧 감사 추적입니다."

### 22. oh-my-openagent `[→ 0]` · 1:00
> "oh-my-openagent — 구 oh-my-opencode에서 개명했습니다. Sisyphus 오케스트레이터가 librarian, oracle, explore 같은 전문 에이전트 열한 종에 병렬로 위임합니다. 주의할 층위 하나 — **LSP와 ast-grep은 에이전트가 아니라, 에이전트들이 쓰는 내장 도구**입니다. 그래서 그림이 두 층입니다."

### 23. oh-my-openagent — 동작 보기 `[→ 8]` · 1:15
> 도입: "작업 하나가 어떻게 분해되고 취합되는지 봅니다." — 줄 읽기.
> 촌평: "위임은 컨텍스트 분리입니다 — 본진의 맥락이 오염되지 않습니다."

### 24. Devin `[→ 0]` · 1:00
> "마지막, Devin. '완전 자율 AI 엔지니어'로 알려졌지만 지금 동작은 이렇습니다 — 작업을 주면 계획 초안을 만들고, **사람이 검토·수정해야 자율 실행이 시작됩니다.** 실행은 머신 스냅샷으로 리셋되는 자기 VM에서, 그리고 기본 사용법이 **병렬 세션 여러 개**입니다."

### 25. Devin — 동작 보기 `[→ 8]` · 1:15
> 도입: "위임의 끝단이 실제로 어떤 모습인지 봅니다." — 줄 읽기.
> 촌평: "위임이 극단이라서, '계획 검토'라는 하네스가 맨 앞에 붙습니다. 스펙트럼의 오른쪽 끝일수록 하네스가 무겁다 — 오늘 명제 그대로입니다."

### 26. 정리 — 위임하고, 조여라 `[→ 4]` · 1:30
> "정리합니다. 점선 부채꼴이 1부 — 위임이 키운 폭. 그 안의 좁은 띠가 2부 — 하네스가 조인 폭." `[→]` "①위임 — 호출을 넘어 일을 통째로 맡긴다." `[→]` "②조임 — 지시문·도구·절차·검증의 4층." `[→]` "③증거 — 완료 선언이 아니라 검증이 일을 끝낸다." `[→]` "**자동화를 한 층 키울 때마다, 하네스도 한 층.** 이 한 문장 가져가시면 됩니다."

### 27. 끝 `[→ 0]` · 0:30
> "우리 팀에 적용할 때의 출발점은 두 가지입니다 — 지금 '호출'로 짜여 있는 자동화 중 '위임'으로 넘길 후보 하나, 그리고 거기에 어떤 층부터 조일지. 질문 받겠습니다."

---

## 압축 경로

**약 37분으로 (−7:30):**
- 1부 9·10장(방법 2·3)은 "SDK는 base_url만, LangChain은 체인 조립" 한 문장씩으로 훑습니다. (−1:30)
- 2부: 동작 보기는 **ponytail, fablize, Devin 세 장만 정독**하고, 나머지 일곱 장은 "구조 장에서 설명한 그대로입니다" 하고 `→` 연타로 최종 상태만 보여 줍니다. (−6:00 내외)

**약 33분까지 줄여야 하면 추가로 (−3:15):**
- 동작 보기 정독을 ponytail·fablize 두 장으로. (−1:15)
- 1부 8장(방법 1)도 훑기, 1부 1·2장(표지·여정) 간략화. (−2:00)
- 2부 15장(스펙트럼)·26장(정리)은 어떤 경우에도 유지 — 서사의 뼈대입니다. 이보다 더 줄여야 하면 본편이 아니라 Q&A를 줄이세요.

## 예상 Q&A (근거는 docs/blindspot/seminar-accuracy-behavior-implementation-notes.md 05:50 출처 목록)

1. **"Copilot Workspace 아직 쓸 수 있지 않나요?"** — GitHub Next 프로젝트 목록에서 아카이브 상태이고 서비스 주소는 404입니다. 명세·계획 중심 철학은 Copilot Cloud Agent가 이어받았습니다. 참고로 Cloud Agent 자체는 GitHub 체인지로그(2026-04) 제목대로 'formerly known as Copilot coding agent' — 즉 coding agent의 새 이름입니다.
2. **"Devin은 완전 자율 아니었나요?"** — Devin 2.0부터 Interactive Planning이 기본입니다. 사람이 계획을 승인해야 자율 실행이 시작되고, 병렬 세션 운용이 기본형입니다.
3. **"oh-my-opencode 아닌가요?"** — 저장소가 oh-my-openagent로 개명됐습니다. 저장소 설명에 'previously oh-my-opencode'라고 명시되어 있습니다.
4. **"Aider 요즘도 쓰나요?"** — 릴리스 속도가 느려져 저속 유지보수 상태입니다. 그래서 '현재 권장'이 아니라 'CLI 에이전트의 원형'으로 소개했습니다.
5. **"LangChain으로 에이전트 짜려면 create_react_agent 쓰면 되죠?"** — 그 API는 deprecated입니다. 현재 표준은 LangGraph 런타임의 create_agent + 미들웨어입니다.
6. **"하네스로 조이면 창의적인 답이 안 나오지 않나요?"** — 조임의 목적은 고정이 아니라 '원하는 결과가 더 자주'입니다. 탐색이 필요한 작업은 일부러 폭을 넓게 둡니다(2부 3장 문구).
7. **"grill-me·fablize는 어디서 받나요?"** — 각각 mattpocock/skills(스킬 폴더), fivetaku/fablize 공개 저장소입니다. ponytail·blindspot-flow는 우리 환경에 이미 설치되어 있습니다.
8. **"폐쇄망에서도 이 자료가 열리나요?"** — 네. 폰트·라이브러리가 전부 저장소에 동봉되어 있어 file://로도 열립니다.
