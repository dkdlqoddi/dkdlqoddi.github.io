# opencode serve 예시 승격 + 전체 발표자료 리뷰 Unknown Unknowns

- 날짜: 2026-08-20
- 입력: docs/blindspot/2026-08-20-serve-methods-and-review-requirements.md
- 스캔 렌즈: conventions / similar-features / integration-points / edge-cases / domain
- 비고: conventions 렌즈는 같은 날 요구사항 단계에서 수행한 스캔을 재사용했다. 사용자 질문은 0건 — 요구사항 단계의 두 답변과 아래 근거로 전 항목이 결정 가능했다. 이 문서의 실측으로 요구사항 확정 2·8을 개정했다(요구사항 문서에 반영 완료).

## 해소된 항목

| # | 발견 (근거 파일:라인 또는 출처 URL) | 구체화된 질문 | 결정 | 결정 주체 |
|---|---|---|---|---|
| 1 | 계단 SVG 4칸이 x=80~875를 차지, 우측 여유 25px. 같은 피치로 2칸 추가 시 385px 초과 (dx index.html:345-379, edge 실측) | 계단 그림에 새 방법 두 칸을 어떻게 넣을까 | 방법 1~4 사다리는 유지하되 칸 간격을 줄여 왼쪽으로 모은다. 방법 5·6은 방법 4와 같은 높이로 오른쪽에 이어 붙인다. 방법 4는 사다리 꼭대기이자 위임 구간의 첫 칸이다. 세부 좌표는 화면 실측으로 확정한다. | 자체 해소 |
| 2 | 코드 물리 상한 3중: 12줄(EVA 700px — 13줄=706px 초과), 관례 54자(파손 임계 ASCII 55.8자), 인쇄 시 pre code 400px 캡(패딩 포함 계산 시 13줄부터 잘림 위험) (deck-base.css:61-62, vendor black.css, edge 실측) | 긴 curl 명령을 코드 칸에 어떻게 넣을까 | 방법 5는 서버 주소를 셸 변수로 빼서 줄을 줄인다. 방법 6은 주소를 파이썬 상수로 둔다. 두 예시 모두 12줄·54자 이내로 맞추고 화면 실측으로 검증한다. | 자체 해소 |
| 3 | 비교표 2열 막대: 등차 연장 시 3열 침범(455>440), 불투명도 0.4부터 대비 3:1 미달, 추상화 단조성도 6행에서 붕괴 (dx index.html:552-610, edge 실측) | 비교표의 추상화 막대를 어떻게 바꿀까 | 막대와 열 이름(추상화 수준)은 유지한다. 방법 5의 막대는 방법 1과, 방법 6은 방법 2와 같은 길이로 그린다 — 호출 코드의 추상화가 실제로 그 수준이다. 모든 행의 불투명도를 0.5 이상으로 하고, 표의 숨은 설명과 aria-label도 6행 기준으로 고친다. | 자체 해소 |
| 4 | 공식 Python SDK(opencode-ai)는 알파(0.1.0a36, 2025-08-27)에서 정지 (https://pypi.org/project/opencode-ai/). 로컬 1.17.18 실측: 전송은 성공하나 응답 필드가 전부 비어 파싱 불능 | 방법 6 예시에 어떤 Python 라이브러리를 쓸까 | requests로 HTTP를 부르는 코드를 싣는다 — 요구사항 확정 2의 조건("실재하면 그것을 쓴다")을 이 실측으로 개정했다. 방법 5(터미널 확인)와의 차별점은 프로그램 통합이다 — 답변 텍스트 추출을 보여 준다. SDK 상태는 대본 예상 질문에 근거와 함께 적는다. | 자체 해소 |
| 5 | POST /session/:id/message는 parts만 필수, model은 선택 (https://opencode.ai/docs/server/ + 로컬 스키마 실측). 생략 실측: 200과 실제 답변 수신 — 무자격 상태에선 무료 기본 모델(opencode/big-pickle)로 해결됨 | curl 예시에 모델 지정을 넣을까 | 뺀다 — 선택 필드임과 생략 시 기본 모델 동작 모두 실측 완료. "모델은 opencode 설정(opencode.json)을 따른다"는 한 줄을 곁들인다. | 자체 해소 |
| 6 | serve는 기본 무인증이고 셸·파일 엔드포인트를 노출, 구버전 CVE-2026-22812 전례 (https://github.com/advisories/GHSA-vxw4-wv6m-9hhh , 로컬 기동 로그) | 보안 경고를 어디에 넣을까 | 방법 5 장의 한 줄 요약에 "기본 무인증, 내 컴퓨터 안(127.0.0.1)에서만"과 비밀번호 환경변수를 적는다. 대본의 같은 장 멘트에도 한 문장 넣는다. 검증 기준 버전 1.17.18도 명기한다. | 자체 해소 |
| 7 | v1 서버 문서에는 안정성 보증도 experimental 표기도 없음. experimental 표기는 v2 문서에만 있음 (https://opencode.ai/docs/server/ , https://opencode.ai/v2/docs) | API 안정성을 어떤 문구로 말할까 | "공식적으로 실험적"이라 쓰면 사실 오류다. "안정성 보증 문구가 없고 소규모 업데이트에서 깨진 전례가 있다"로 쓴다. | 자체 해소 |
| 8 | 1부 현 15:30 + 2부 28:00 = 총 43:30, 45분 예산에 여유 1:30 (docs/2026-08-seminar-script.md:19, edge 실측 합산). 신규 2장을 1:00씩 잡으면 45:30으로 초과 | 늘어나는 발표 시간을 어떻게 흡수할까 | 신규 2장을 각 0:45로 잡아 총 45:00에 맞춘다. 기존 장의 시간은 바꾸지 않는다. 압축 경로 두 개의 머리글 수치(절감량·결과 시간)를 재계산하고, 첫 경로에 "방법 5·6 훑기(각 0:20)"를 더한다. | 자체 해소 |
| 9 | 대본 화살표 표기는 fragment 클래스 수가 아니라 고유 인덱스 수 기준 (docs/2026-08-seminar-script.md:39·:54, dx index.html:289) | 대본의 화살표 횟수와 장 번호를 어디까지 고칠까 | 현행 7장(계단)은 [→ 3]을 [→ 5]로 고친다. 현행 12장(비교표)은 갱신 후 14장이 되며 [→ 4]를 [→ 6]으로 고친다. 신규 2장(갱신 후 12·13장)은 [→ 0]이다. 대본 헤더는 현행 12~14장을 14~16장으로 민다. | 자체 해소 |
| 10 | "4가지" 갱신 대상에 주석 6곳이 더 있음: dx:33·:341·:548·:620·:655, philosophy:49 (similar 스캔) | 주석도 갱신 체크리스트에 넣을까 | 넣는다 — 주석은 다음 작업자가 읽는 규칙 문서라서 거짓 참조를 남기면 안 된다. | 자체 해소 |
| 11 | 1부 미터는 원문자 ①~④, 2부는 평문 1~5로 이미 구분됨 (dx:386, philosophy:690) | 6칸 미터의 숫자 표기를 무엇으로 할까 | 원문자 ①~⑥을 유지한다 — 1부 고유 표기라 2부와 구분된다. ⑤⑥ 글자가 제대로 보이는지 화면으로 확인한다. | 자체 해소 |
| 12 | deck-base.css:73-74의 !important가 1부 미터·표지 위 눈썹글의 고정폭 글꼴 지정을 무력화. 2부는 인라인 !important로 이미 우회 (philosophy:60-62) | 1부 미터 글꼴을 어떻게 복구할까 | 이 글꼴 규칙에 한해서는 공유 CSS를 고치지 않는다 — 다른 덱 회귀 위험 회피. 대신 2부와 같은 인라인 우회 규칙을 1부 안에 넣는다. | 자체 해소 |
| 13 | 코드 블록이 브라우저 인쇄에서 대비 1.35:1로 사실상 안 보이고, 13줄부터는 인쇄에서 잘릴 위험 (deck-base.css:195-213, edge 실측) | 코드 인쇄 결함을 어디서 고칠까 | 전 덱 공통인 deck-base.css 인쇄 블록에 흰 배경·검정 글자 복구와 잘림 해제를 넣는다. 캐시 표식을 ?v=2로 올리고 5개 덱 링크를 함께 고친다. | 자체 해소 |
| 14 | design-ax 37곳·integrated 135곳의 하드코딩 색 중 129곳이 흰 종이에서 대비 3:1 미만 (integration 실측, 선행 이월 항목) | 인쇄에서 안 보이는 두 덱을 어떻게 고칠까 | 화면은 그대로 두고 각 덱에 인쇄 전용 색 덮어쓰기 규칙만 넣는다. 전면 재작성은 하지 않는다 — 비용 대비 결과가 같다. 재작성 경로는 보고서에 남긴다. | 자체 해소 |
| 15 | 복사 버튼이 키보드 포커스 때 투명 상태로 남음 — 보이게 하는 규칙이 마우스 올림뿐 (code-copy.css:18-25) | 복사 버튼 접근성 결함을 고칠까 | 고친다 — 포커스가 닿으면 보이게 하는 규칙을 code-copy.css에 추가한다. | 자체 해소 |
| 16 | 랜딩 인쇄 규칙의 대상 이름 오타로 은하 캔버스·연결선이 종이에 그대로 나감 (index.html:32-33 vs styles.css:349) | 랜딩 인쇄 결함을 고칠까 | 고친다 — 올바른 이름으로 숨김 규칙을 바로잡는다. styles.css의 캐시 표식은 현행 ?v=2 그대로 둔다(시각 대개편 아님). | 자체 해소 |
| 17 | galaxy3d.js의 동작 축소 방어가 부분적 — 행성 자전과 셰이더 시간은 계속 돎 (scripts/galaxy3d.js:287·:296) | 동작 축소 방어를 완성할까 | 한다 — 두 곳에 기존과 같은 방식의 가드를 추가한다. CLAUDE.md의 "모든 모션 정지" 계약을 코드가 지키게 한다. | 자체 해소 |
| 18 | integrated 덱 잔재: SMIL 애니 1건(:624), 사문화 CSS(:43-47의 :root 블록과 뒤따르는 글꼴 규칙), 중복 선언(:40), 가드 없는 애니(동작 축소 시 점이 엉뚱한 곳에 고정), 돌아가기 링크 표기 상이(:788) | integrated 덱 잔재를 이번에 정리할까 | 전부 정리한다 — 저장소 규칙 위반(SMIL 금지)과 동작 결함이 섞여 있고 수정 비용이 작다. | 자체 해소 |
| 19 | 2부 터미널 장 한 줄만 프롬프트 기호가 이중으로 렌더됨: "❯ $ aider …" (philosophy:735 + :68) | 이중 프롬프트 한 줄을 고칠까 | 고친다 — 코드 안의 $ 기호를 지워 다른 12줄과 표기를 맞춘다. | 자체 해소 |
| 20 | 과거 퀴즈·보고서 속 "14장/41쪽/네 가지" 수치가 이번 변경으로 낡아짐 (quiz/2026-08-20-seminar-script.html:24·:43 등). README:6의 "4단계"는 덱 추가 절차 설명이라 별개 사안 (integration 스캔) | 과거 심사 기록을 소급 수정할까 | 하지 않는다 — 퀴즈·보고서는 그 시점의 기록으로 보존한다. 발표자가 실제로 보는 살아있는 문서(대본·덱·목록)만 갱신한다. README도 이번 변경과 무관하므로 갱신 대상이 아니다. | 자체 해소 |
| 21 | 새 코드 블록에 &가 들어가면 HTML 실체참조가 필요, $ 프롬프트 기호는 복사 오염 유발 (edge 스캔, code-copy.js:19). 코드 종류 표시는 하이라이트 플러그인이 없어 렌더에 영향 없음 | 새 코드 블록의 표기 규칙은 무엇인가 | 프롬프트 기호 없이 순수 명령만 적는다. &와 <만 실체참조로 바꾼다. 코드 종류 표시는 방법 5가 bash, 방법 6이 python이다. | 자체 해소 |
| 22 | serve가 어느 폴더를 프로젝트로 보는지 공식 문서에 없음 (https://github.com/anomalyco/opencode/issues/14445). 로컬 실측: 응답의 path.cwd가 서버를 띄운 폴더를 가리킴 | 서버의 기준 폴더 설명을 슬라이드에 넣을까 | 넣지 않는다 — 공식 근거가 없는 단정은 피한다. 실측 결과를 대본 예상 질문에 적는다. | 자체 해소 |
| 23 | 랜딩 카드 정렬이 같은 날짜 두 덱의 순서를 배열 원순서에 의존 (main.js:78-84, integration 스캔) | 같은 날짜 덱의 정렬 의존을 지금 고칠까 | 고치지 않는다 — 현재 결과가 의도와 일치해 결함이 아니다. 같은 날짜 덱이 또 추가되면 재검토한다. | 자체 해소 |
| 24 | 도메인 부가 사실 3건: 폐쇄망에서 나가는 연결 2종(모델 목록 조회 + LLM 호출), JS SDK만 본체와 같은 버전(1.18.18), 여러 인스턴스의 세션 저장소 간섭 (domain 스캔 출처 URL) | 부가 사실 3건을 발표에 반영할까 | 슬라이드에는 싣지 않는다. 대본 예상 질문 절에 근거와 함께 더한다 — 폐쇄망 축은 대본이 이미 다루는 주제다. | 자체 해소 |

## 미해소 항목

| # | 질문 | 보류 이유 | 재방문 시점 |
|---|---|---|---|
| 1 | 인쇄 규칙 `svg { display:block !important }`가 나중에 SVG 제목 태그를 유령 글자로 찍는 문제를 지금 손볼까 | 현재 SVG에 제목 태그가 0건이라 발현 조건이 없다 | 덱 SVG에 title/desc를 도입하는 작업이 생길 때 |
| 2 | 2부 터미널 장들의 실제 화면 높이가 상한(660px — 전부 단계 공개가 있는 장)을 넘는지 | 정적 계산으로는 과대평가 소지가 있어 판단 불가 | 이번 구현의 화면 실측(헤드리스 브라우저) 단계 |

## 스캔 원본 요약

### conventions

(요구사항 단계 스캔 재사용) slides.json 4엔트리 전부 실물 존재, 대상 슬러그는 dx-vs-ax-automation. 덱은 평면 14장·id 0개, auto-animate 짝은 :168↔:221 한 곳(pipe-n/pipe-a data-id). 코드 슬라이드 5단 골격(meter→visually-hidden→mini-diag→pre>code.python→small) 4장 공통, 11~12줄·54자, 첫 줄 설치 주석·끝 줄 출력 주석. 링크 순서 계약 5덱 동일(black.css 뒤·인라인 앞), pre>code면 복사 버튼 자동. "4가지" 하드코딩: 덱 15곳 안팎 + slides.json:17 + 대본 6곳. 덱 간 링크는 디렉터리 URL만(#/N 참조 0건). 대본은 장 번호·제목·[→ n]·총 장수·쪽수를 명시(:3·:15·:42-58·:164·:169). 산출물 4종 세트(덱+구현노트+보고서+퀴즈 HTML)가 커밋 관례.

### similar-features

방법 1(:384-419)·방법 4(:508-545)가 복제 정본 — mini-diag는 전부 viewBox 0 0 900 65, fill="none" stroke="currentColor", accent/dim/bright 클래스만 사용(직접색 0건), fragment 0개 → 높이 상한 700px. 미터 CSS는 두 덱에 문자 단위 복제(dx:33-37↔philosophy:49-53), 구분은 글리프(①~④ vs 1~5)와 라벨(추상화 vs 자율성)뿐. 비교표(:548-618): 행 피치 62, `<g class="fragment fade-up">` 행 단위, 2열 막대 45→180 등차. 계단(:341-381): x=80/285/490/695 폭 180 피치 205 — 6칸 등피치 불가(가용 25px). 셸/curl 코드 선례 없음(class="python" 4건뿐), sample JSON은 클래스 없음. $ 프롬프트는 philosophy:735 한 곳(이중 프롬프트 렌더 — 수정 후보). 2부는 1부의 "4가지/추상화"를 본문에서 인용하지 않음(주석 philosophy:49만). 대본 장 헤더 형식 `### N. 제목 [→ n] · M:SS` + 인용 멘트. 코드 12줄 한계는 vendor black.css의 pre code max-height:400px 하드 캡(패딩 포함 12.35줄 → 13줄부터 잘림 위험): 방법 1~3이 정확히 12줄. 인쇄 방어가 pre만 풀고 pre code는 안 풀어 초과분은 종이에서도 잘림.

### integration-points

랜딩 4자산 계약 건전 — 슬라이드 추가는 랜딩 무영향, slides.json:17 문구만 갱신 대상. deck-base.css?v=1 참조 5덱 동일 — 공유 CSS 수정 시 ?v=2 범프+5링크 동시 수정 강제. code-copy.css에 :focus 계열 0건(WCAG 2.4.7 위반, 랜딩 styles.css:268은 정상 처리 — 저장소 내 불일치), code-copy 링크에는 ?v 표식 자체가 없음(10분 캐시로 수렴). 5덱 규칙 스냅샷: viewport·color-scheme·uppercase 취소·view-transition·reduced-motion·print 전부 정상, 선행 "파손 3종" 재현 안 됨(148886e/a53a734에서 수리). integrated 실측 건전(section 18/18, mojibake 0)이나 잔재 4건: html 배경 중복(:40), 사문화 :root(:43-47)와 글꼴 규칙, 돌아가기 링크 ../../index.html(:788, 타 덱은 ../../), SMIL 1건(:624). 1부↔2부 결합: 미터 CSS 복제쌍, 상호 참조 주석 dx:33↔philosophy:49(축 개명 시 philosophy:49 동기화 필수), 표지 eyebrow·크로스 링크는 무영향. 인쇄 색 이월 항목: philosophy 0건(해소), design-ax 37건 중 30건·integrated 135건 중 99건이 흰 종이 대비 3:1 미만(#fcfcfb 1.03:1, #00ffff 1.25:1 등). 랜딩 인쇄 블록 .galaxy-bg는 실제 클래스 .galaxy-3d-bg와 불일치(3D 도입 전 잔재) → 인쇄 시 캔버스·#hud-lines 노출. galaxy3d.js 동작 축소 가드가 회전·혜성·줌만 커버, :287 자전·:296 셰이더 시간 미가드, rAF 무조건 지속. 퀴즈 3건·보고서 2건·README는 낡은 수치 잔존 예정(소급 수정 대상 아님 판정). .nojekyll·서브모듈 HTTPS·매니페스트 전부 정상. 현재 브랜치 main — 작업 전 분기 필수.

### edge-cases

미터 6칸 안전(348px/960px, nowrap flex — 줄바꿈 불가 구조). 단 deck-base:73-74 `.reveal p` !important가 미터·eyebrow의 --font-mono를 무력화(1부만 미해결, 2부는 :60-62에서 우회 완료). 계단: 등피치 6칸 불가(위 similar와 일치), 세로는 viewBox 380까지 여유. 비교표 6행 높이 안전(528px/660px, 7행도 가능), 막대 연장 시 3열 15px 침범 + opacity 0.4부터 3:1 미달 + 단조성 붕괴. 코드 상한 3중 실측: 12줄(677px, 여유 23px — 13줄 초과), ASCII 55.8자(한글 1자=1.667자), 인쇄 pre code 400px 캡(패딩 포함 13줄부터 잘림 위험). white-space:pre라 소프트랩 없음 — 넘친 줄은 화면 가로 스크롤, ?print-pdf에선 소리 없는 잘림. 복사 온전성 확정: innerText는 code만 읽고 버튼은 형제라 오염 없음, 백슬래시 연속행 안전. 이스케이프는 &·<만 필요, $·따옴표·중괄호는 그대로 안전(.nojekyll로 Liquid 미실행). 삽입 지점 :546-548 완전 격리(스타일·스크립트·데이터 속성 없음), 해시 밀림(12~14→14~16)은 참조 0건 + 수용 선례 2건. 대본 [→ n]은 고유 인덱스 수 규약(6장 클래스 3·인덱스 2 → [→ 2]). 1부 합계 실측 15:30. Ctrl+P에서 pre/code 색 복구 규칙이 reveal·deck-base·덱 어디에도 없음 → 흰 종이 위 #dedede = 1.35:1(2부 .term은 :102-107에서 복구 완료 — 1부만 누락). 갤럭시·폰트 폴백·head 계약은 5문서 전부 건전. integrated 무가드 애니는 동작 축소 시 흐르는 점이 엉뚱한 위치에 고정 표시(1부 travel-dot 패턴과 불일치).

### domain

serve 기본 127.0.0.1:4096, 기본 무인증(OPENCODE_SERVER_PASSWORD로 Basic Auth, 사용자명 기본 opencode), OpenAPI 3.1은 GET /doc (https://opencode.ai/docs/server/). 최소 흐름: POST /session({} 가능, ses_… 반환) → POST /session/:id/message(parts만 필수, model:{providerID,modelID}는 선택·중첩 객체, 동기 응답 {info,parts}) — 비동기는 prompt_async. 로컬 1.17.18 실증: 기동 로그 무인증 경고, /doc 경로 162개, 세션 생성·삭제 왕복 성공, model 생략 요청이 200 + 실제 답변("2") 수신(기본 모델 opencode/big-pickle, 비용 0), 응답 텍스트는 parts[]의 type=="text" 항목, info.path.cwd = 서버 기동 폴더, /api/* v2 경로 병존. Python SDK opencode-ai: PyPI 0.1.0a36(2025-08-27, --pre 필요), 로컬 실측 — session.create()는 성공, session.chat()은 전송되나 반환 객체 필드가 전부 None(응답 스키마 불일치로 파싱 불능). JS SDK @opencode-ai/sdk 1.18.18은 본체와 락스텝(대비 축). v1 문서에 안정성 보증·experimental 표기 없음, v2만 명시적 베타/experimental, 1.15.13에 /message 500 회귀 전례. 보안: CVE-2026-22812(1.0.216 미만, 무인증 셸/PTY/파일 RCE, CVSS 8.8) — 1.17.18은 패치 후이나 엔드포인트 성격은 동일, --mdns는 0.0.0.0 노출. 폐쇄망: models.dev 조회+LLM API 두 아웃바운드, 에어갭 미지원(열린 이슈). 다중 인스턴스는 동일 project_id로 세션 DB 공유 간섭, 한 서버 다중 세션은 분리 정상. 저장소 정본은 anomalyco/opencode(GitHub 조직 opencode-ai의 동명 저장소는 무관한 아카이브 프로젝트).
