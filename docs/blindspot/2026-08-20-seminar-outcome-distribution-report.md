# 세미나 결과 분포 시각화·문구 교정 작업 보고서

- 날짜: 2026-08-20
- 기준: main (1497985) → feat/seminar-outcome-distribution (71ee485)
- 퀴즈: docs/blindspot/quiz/2026-08-20-seminar-outcome-distribution.html — 통과 전 머지 금지
- 읽는 법: 코드를 모르는 분은 '요약'과 '스크린샷/데모'까지만 읽으면 됩니다. 그 아래는 개발자와 AI를 위한 상세입니다.

## Human 섹션

### 요약

발표 자료 2부작의 결과 그림들이 바뀌었습니다.
전에는 하네스(모델을 감싸는 운영 장치)가 결과의 폭을 "좁힌다"만 보였습니다.
이제는 조여진 띠가 잘된 결과 쪽으로 올라가, 결과가 "좋아진다"까지 보입니다.
결과 점도 두 종류로 갈라졌습니다: 잘된 결과는 청록 원(●), 망한 결과는 빨간 엑스(✗)입니다.
스킬 다섯 장에는 "적용 전/적용 후" 분포를 비교하는 작은 그림이 하나씩 새로 들어갔습니다.
은어 "갈굼/갈궈줘"는 "압박 질문"으로, 비문 "남은 아는데"는 "남들은 아는데"로 고쳤습니다.
발표 대본도 화면에 맞춰 갱신했고, 발표 시간과 장 수는 그대로입니다.

옛날에 발행한 퀴즈 두 건은 "띠가 가운데로 조여진다", "남은 아는데"를 그대로 담고 있습니다.
발행 기록은 고치지 않는 원칙에 따라 그대로 두며, 현재 화면과 다른 이유가 바로 이번 변경입니다.

### 스크린샷 / 데모

- 확인 방법: 저장소 폴더에서 `python3 -m http.server 8000` 실행 뒤 아래 주소를 엽니다.
- 하네스 모핑(2→3→4장): http://localhost:8000/slides/ai-agent-skills-philosophy/#/1 에서 화살표로 진행
- 스킬 소형 그림: http://localhost:8000/slides/ai-agent-skills-philosophy/#/4 (ponytail, 이후 #/6 #/8 #/10 #/12)
- 정리 장: http://localhost:8000/slides/ai-agent-skills-philosophy/#/26
- 1부 브리지: http://localhost:8000/slides/dx-vs-ax-automation/#/15 (화살표 1회로 예고 띠 표시)
- 구현 중 헤드리스 브라우저로 렌더링·높이·범례 색을 실측했습니다(캡처 파일은 저장소에 남기지 않음).

### 리뷰 포인트 (개발자용)

- `slides/ai-agent-skills-philosophy/index.html:149-167` — 모프 1장 점 12개 재부호화. ✗는 `<text data-id>` — reveal 6.0.1 짝 키가 `nodeName:::data-id`라 세 장에서 태그·font-size(22) 불변이 계약.
- `:186-231` — 모프 2장: 띠 우변 120/180·130/170·140/160(중심 150, 꼭짓점 고정), 4층 도형 동반 이동, 바깥 점 구성(위 ● d4 / 아래 ✗ d3·d6·d11 / 반려 d8).
- `:253-267` — 모프 3장: 리본 ● 8 + "걸러진 시도" ✗ 4(기준선 87/88/88/86 — 이름표 배경판 y93과 5~7px 여유).
- `:37-40` — 신규 `.reveal p .accent/.danger` 색 규칙: 본문 범례용. deck-base의 SVG 한정 정의를 보완(변경 분석에서 적발된 무채색 결함의 수정).
- `:358` `:445` `:545` `:641` `:735` 부근 — mini-diag 5개: 캡션·산포값(±12/10/8/6/4) 외 구조 동일, fragment 없음.
- `:315` — ponytail 뷰박스 252→236(실측 694px, 여유 6px — 임계), `:516` — blindspot-flow 220→200(673px).
- `slides/dx-vs-ax-automation/index.html:812-836` — 브리지: 점 ●5/✗3 재배치, 예고 점선 띠 중심 158→135, 라벨 배경판 y106→74.
- `docs/2026-08-seminar-script.md` — §16(1부)·§2·3·4·5·7·9·11·13·27 낭독 갱신, `[→ n]`·시간 불변(교차 대조 통과).

## Agent 섹션 (AI 인수인계용)

### 의도 (Intent)

사용자 요청 4건(어색한 한글 교정, 하네스의 "결과 개선" 서사, 좋/나쁨 직관화, 스킬별 전/후 try 분포)을 기존 덱 관용구 안에서 구현. 결정 정본: requirements(개정 2건 포함)/unknowns(해소 26)/explainer(구현 반영 6항).

### 제약 (Constraints)

- auto-animate 짝 키 = `nodeName:::data-id` → ✗로 바뀐 d3·d6·d8·d11은 세 장 모두 `<text>`, font-size 22 고정. 나머지 8개는 `<circle>` 유지.
- 허용 글리프 U+2717/U+2713/U+25CF만(Montserrat 부재, Pretendard 보유 — file:// 오프라인 보증).
- 색은 클래스+currentColor만(인쇄 검정 복구·색 애니메이션 조건). `fill="#…"` 유입 0건 유지.
- 높이 상한: fragment 장 660px / 무 fragment 장 700px. 소형 그림은 fragment 없음 → 대본 `[→ 0]` 불변.
- 점에는 `fill="none"` 금지(자동 배경판 오폭), 소형 그림 칸 테두리는 `rect fill="none"`로 배경판 이득.
- 기준선: 모프 1·2장 y=200, 모프 3장 리본 중심 y=55, 정리 y=153, 브리지 y=158, 소형 칸 y=50. 기호는 기준선 위 ●/아래 ✗, 모프 체인에서는 첫 장 판정으로 고정.

### 검토한 엣지케이스

| 엣지케이스 | 처리 |
|---|---|
| 본문 범례 ●/✗가 SVG 밖이라 무채색 | `.reveal p .accent/.danger` 규칙 신설, 실측으로 청록/빨강 확인 (변경 분석 적발 → 정정) |
| 모프 3장 ✗가 이름표 배경판(불투명, y93~)에 가림 | 기준선 2~3px 상향(87/88/88/86), 여유 5~7px 확보 |
| 모프 1→2에서 ✗ 일부가 위로 이동하는데 대본이 "아래로" | 대본을 위치 서술("띠 밖 아래에 남고")로 정정 |
| 긴 본문 문장이 두 줄로 꺾여 706px 초과 | 모프 2장 본문을 두 줄로 재배분(679px), 방어 문구 "하나로 고정하진 않습니다" 유지 |
| ponytail 장 705px 초과 | 기존 그림 뷰박스 252→236 + 캡션 압축(694px) |
| 소형 그림 제목과 산포 상단 점 겹침 | 제목 왼쪽 정렬(x30/x480), 적용 후 점 x545~840 |
| 지도 장에서 ✗(fs22 고정)가 ●(r4)보다 큼 | 수용 — 짝 흔들림 방지 계약의 대가, 설계 문서 기록 |
| 옛 퀴즈 2건이 새 화면과 모순 | 발행 동결 유지, 본 보고서 요약에 사유 명시 |

### 검증 결과

check-runner 8종 전부 통과: slides.json 유효(4항목), dir↔index 4/4, 금지 패턴 0, .nojekyll·sample 유지, 은어·비문 잔존 0(동결 문서 제외), svg:설명 = 2부 21:31·1부 15:15, 하드코딩 색 0, fragment 수 main 동일(89/21). 헤드리스 실측: 전 장 높이 ≤700px(최대 ponytail 694), 콘솔 오류 0, 범례 색 rgb(0,221,221)/rgb(240,80,80), 글리프 정상 렌더. 미실시(권장 수동): `?print-pdf` 실제 인쇄 육안, 동작 축소 육안 — 이전 사이클과 동일하게 이월.

### 의도적 범위 제외

- 옛 퀴즈·과거 blindspot 문서의 옛 서술 수정(발행 동결) — 본 보고서에 사유 한 줄로 갈음.
- 제품 5종 장의 분포 그림(요청이 스킬로 한정), 해부도 장(부위 지도 역할 유지).
- slides.json 카드 문구·meta description·1부 여정 지도 문구("폭을 조인다" 프레이밍 유지로 그대로 유효).
- design-ax-transition 덱의 "구속구" 용어 통일(별도 사이클), SVG 라벨 24px 하한 미달의 전면 해소(기존 이월 항목).

### 구현 노트 요약

- 이탈 1: 소형 그림 제목 왼쪽 정렬 + 적용 후 점 x545~840 (겹침 실측 근거).
- 이탈 2: 정리 장 "하네스가 조인 폭 — 2부" 라벨 y194→146 (띠 추종) — **사용자 확인 필요, 아래 게이트에서 질문**.
- 이탈 3: 모프 2장 본문 두 줄 재배분, ✗ 글자 크기 소형 16→18, ponytail 뷰박스 축소 — 높이·가독 실측 근거, explainer 구현 반영 절에 기록.
- 보수적 선택: 서열 문구는 양 끝 2장만 표기(중간 3장은 층 문구만), 브리지 ✗ 크기 22로 등급 2개 유지.
- 변경 분석 적발 후 정정 4건: 범례 무채색, 모프 3장 ✗ 여백, 대본 방향 서술, 설명문 "안팎" — 모두 반영 완료.
