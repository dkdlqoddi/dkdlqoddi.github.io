# 우주 유영 테마 리디자인 구현 노트

- 시작일: 2026-07-27
- 연관 문서: `docs/blindspot/2026-07-27-space-theme-requirements.md`, `docs/blindspot/2026-07-27-space-theme-unknowns.md`, `docs/blindspot/2026-07-27-space-theme-explainer.md`

<!-- 결정 시점마다 아래 형식으로 append. 사후 재구성 금지. -->

## 2026-07-27 15:04 — 글꼴 원본 확보 경로와 내장 CSS 생성

- 결정: Pretendard Variable v1.3.9 woff2를 jsDelivr의 `packages/pretendard/dist/...` 경로에서 받았다. sha256 `9599f12f...`를 CSS 주석에 기록했다. base64 내장 CSS(2,744,520바이트)와 LICENSE를 `vendor/pretendard/`에 넣었다.
- 이유: 리서치가 제시한 `dist/...` 경로는 404였다(저장소가 모노레포로 개편됨). 크기가 조사값 2,057,688바이트와 정확히 일치해 동일 파일임을 확인했다.
- 검토한 대안: npm CDN 경로, GitHub raw — 첫 대안 경로가 성공해 사용하지 않음.
- 보수적 선택 여부: 아니오
- 계획과의 이탈: 없음 (다운로드 경로만 다름, 산출물은 설계대로)
- 사용자 확인 필요: 아니오

## 2026-07-27 15:10 — 별 개수: 랜딩 80개, 덱 34개

- 결정: 랜딩 별은 2px 40개 + 1px 40개 = 80개(설계 범위 60~90 내). 덱 별은 2px 18개 + 1px 16개 = 34개로 랜딩보다 줄이고 더 흐리게(0.4/0.22) 했다. 상단·하단 가장자리(102~103vh) 여유 별로 drift 공백을 메웠다.
- 이유: 설계가 "내용 집중을 위해 발표 자료 장식 최소화"를 요구한다. 랜딩 기준 개수를 덱에 그대로 쓰면 본문 뒤가 산만하다.
- 검토한 대안: 덱도 80개 — 기각(집중 방해).
- 보수적 선택 여부: 예 (덱 장식을 더 줄이는 쪽)
- 계획과의 이탈: 없음 (설계는 랜딩 개수만 60~90으로 못박음)
- 사용자 확인 필요: 아니오

## 2026-07-27 15:12 — 우주선 실루엣 형태

- 결정: 인라인 SVG로 캡슐형 본체 + 위아래 꼬리날개 + 엔진 노즐 + 창(배경색 링) + 항적 점선 5요소 실루엣을 그렸다. 창의 링 모티프는 파비콘의 링과 통일했다.
- 이유: "단순한 흰 실루엣" 요구. 파비콘과 시각 언어를 맞추면 정체성이 이어진다.
- 검토한 대안: CSS 도형 조합 — 곡선 표현이 장황해 기각(설계 문서에 기록됨).
- 보수적 선택 여부: 아니오
- 계획과의 이탈: 없음
- 사용자 확인 필요: 아니오 (형태는 스크린샷 검증 단계에서 함께 확인)

## 2026-07-27 15:14 — sample의 인쇄 방어는 장식 숨김만

- 결정: sample의 @media print는 별·우주선 숨김만 넣고, 검정 되돌림 규칙은 주석 안내로 대체했다.
- 이유: sample에는 직접 만든 도형(div/span)이 없다. 없는 대상에 규칙을 넣으면 죽은 코드다. 대신 주석이 "도형이 있으면 이렇게 추가하라"고 안내한다.
- 검토한 대안: cds와 동일한 규칙 전문 복사 — 기각(대상 없음).
- 보수적 선택 여부: 아니오
- 계획과의 이탈: 없음
- 사용자 확인 필요: 아니오

## 2026-07-27 15:15 — 규칙 문서 갱신 시 README에 덱 머리 선언 항목 추가

- 결정: README "자주 걸리는 것"에 "덱 머리에 어두운 화면 선언 필요" 항목을 새로 추가했다. CLAUDE.md에도 같은 규칙(Declare darkness before JS)을 추가했다.
- 이유: 이번에 새로 생긴 함정(흰 번쩍임)은 sample을 복사하지 않고 덱을 만들면 재발한다. 규칙 문서의 존재 이유가 정확히 이런 함정 기록이다.
- 검토한 대안: sample 주석에만 기록 — 기각(CLAUDE.md 덱 규칙 절이 이런 함정의 공식 위치).
- 보수적 선택 여부: 아니오
- 계획과의 이탈: 없음 (요구사항 11번의 "세 문서" 범위 안)
- 사용자 확인 필요: 아니오

## 2026-07-27 15:25 — 구현 검증 결과 (열린 질문 2건 해소)

- 결정: 검증 통과로 판단하고 커밋을 진행한다.
- 이유: 실측 결과 —
  - file:// 오프라인(Chromium 실측): Pretendard 로딩 true, 배경 #0e0e0e, 20장 정상. base64 내장이라 글꼴 네트워크 요청 자체가 없으므로 Firefox의 상위 폴더 차단도 원리적으로 무관해짐. (열린 질문 1 해소)
  - keep-all 적용 후 20장 전체 가로 넘침 스캔: 실질 0건. (열린 질문 2 해소)
  - 움직임 줄이기 에뮬레이션: 우주선·별 animation:none 확인.
  - 인쇄 에뮬레이션: .box 글자·테두리 rgb(0,0,0), 별·우주선 display:none 확인.
  - 콘솔 오류·경고 0건. 모바일(390px) 가로 스크롤 없음. 랜딩·덱 표지·그림 2장·단계 pill·마무리·sample·404 스크린샷 육안 확인.
- 검토한 대안: 없음 (검증 항목은 설계 문서의 열린 질문 그대로)
- 보수적 선택 여부: 아니오
- 계획과의 이탈: 없음
- 사용자 확인 필요: 아니오 (프로젝터 리허설만 발표자 몫으로 남음)
