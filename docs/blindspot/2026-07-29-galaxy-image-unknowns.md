# 단일 은하계 이미지 배경 변경 Unknowns

- 날짜: 2026-07-29
- 입력: "뒷 배경에 멋진 1개의 galaxy가 화면 전체를 차지하도록 수저해주세요. 원들의 모임이 아니라 실제 galaxy 이미지처럼 보이도록 수정해주세요."
- 스캔 렌즈: conventions / integration-points / edge-cases (직접 수행)

## 해소된 항목

| # | 발견 (출처) | 구체화된 질문 | 결정 | 결정 주체 |
|---|---|---|---|---|
| 1 | `conventions` | 외부 자원(CDN) 금지 및 파일 오프라인 실행(file://) 호환성 원칙 | 실제 은하 이미지를 AI로 생성한 뒤 `assets/galaxy.jpg` 로 저장소 내부에 복사하여 상대 경로로 불러옴. | 자체 해소 |
| 2 | `integration-points` | 기존 캔버스 제거 및 이미지 적용 방법 | 기존 `<canvas class="galaxy-bg">` 및 `scripts/galaxy.js` 파일을 모두 삭제하고, CSS `background-image` 또는 `<img>` 태그에 CSS 애니메이션(`rotate`)을 걸어 무한 자전하도록 구현. | 자체 해소 |
| 3 | `edge-cases` | 직사각형 이미지 회전 시 모서리 잘림 및 해상도 대응 | 이미지를 `150vmax` (화면 대각선보다 크게)로 키우고 화면 정중앙(`top:50%, left:50%, transform: translate(-50%, -50%)`)에 배치하여 어떤 화면 비율에서도 회전 시 모서리가 화면 안에 들어오지 않도록 함. | 자체 해소 |

## 미해소 항목 (사용자 확인 필요)

| # | 질문 | 보류 이유 |
|---|---|---|
| 1 | **이미지 확인**: 배경으로 사용할 은하수 이미지를 AI로 생성해 보았습니다. 이 이미지를 그대로 사용해도 괜찮을까요? (아래 이미지 참고) | 시각적인 만족도는 사용자의 주관적인 판단이 필요함. |

### 🌌 제안하는 배경 이미지
![제안하는 은하수 이미지](file:///c:/Workspace/dkdlqoddi.github.io/assets/galaxy.jpg)
