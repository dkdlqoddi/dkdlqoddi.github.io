# 세미나 덱 콘텐츠 3종 추가 작업 보고서

- 날짜: 2026-08-20
- 기준: main (e5f08e6) → feat/seminar-content-additions 작업 트리 (미커밋)
- 퀴즈: docs/blindspot/quiz/2026-08-20-seminar-content-additions.html — 통과 전 머지 금지
- 읽는 법: 코드를 모르는 분은 '요약'과 '스크린샷/데모'까지만 읽으면 됩니다. 그 아래는 개발자와 AI를 위한 상세입니다.

## Human 섹션

### 요약

발표 자료 2부작에 세 가지 내용이 추가되었습니다. 첫째, 1부에 "HTTP 왕복" 슬라이드가 새로 생겼습니다. 내 명령이 호출자를 거쳐 사내 LLM까지 갔다가 돌아오는 원리를 그림 하나로 보여 줍니다. 둘째, 2부의 blindspot-flow 슬라이드에 맹점과 미지의 미지, 두 개념의 한 줄 정의가 들어갔습니다. 셋째, 2부에 "결과물 해부도" 슬라이드가 새로 생겼습니다. 망한 결과와 잘된 결과를 나란히 놓고, 부위 다섯 개를 스킬 다섯 개와 하나씩 짝지어 보여 줍니다.

발표 시간은 45분 45초에서 47분 15초로 늘었습니다. 45분을 지켜야 하면 새 슬라이드 2장을 가장 먼저 건너뛰는 경로가 대본에 준비되어 있습니다(44분 55초). 발표 대본의 장 번호, 시간 합계, 유인물 쪽수(45쪽)도 함께 갱신되었습니다.

### 스크린샷 / 데모

- 확인 방법: 저장소 폴더에서 `python3 -m http.server 8000` 실행 뒤 아래 주소를 엽니다.
- 1부 새 슬라이드: http://localhost:8000/slides/dx-vs-ax-automation/#/7
- 2부 보강 슬라이드: http://localhost:8000/slides/ai-agent-skills-philosophy/#/10
- 2부 새 슬라이드: http://localhost:8000/slides/ai-agent-skills-philosophy/#/14
- 구현 중 헤드리스 브라우저 캡처로 세 슬라이드의 렌더링을 확인했습니다(캡처 파일은 저장소에 남기지 않음).

### 리뷰 포인트 (개발자용)

- `slides/dx-vs-ax-automation/index.html:411-457` — 신설 8장(HTTP 왕복). viewBox 900×260, fragment 2개, 응답선은 dim 점선 march(1부 주기 4,3), 각주 `class="small dim"`. 이후 주석 번호 9~17로 밀림.
- `slides/ai-agent-skills-philosophy/index.html:552-556` — blindspot-flow 요약 상자 3→5항목. 정의 2줄이 각 한 줄로 렌더되는지(605px 실측, 상한 700px).
- `slides/ai-agent-skills-philosophy/index.html:642-699` — 신설 10장(결과물 해부도). 지도 장 패널 규격(x=50/470, 폭 390) 재사용, 배지 슬롯 x=780/820, 좌우 제목 dim. 이후 주석 번호 11~18로 밀림.
- `docs/2026-08-seminar-script.md:3-4,15,19,71` — 머리말 수치(17장/28장, 47:15, 45쪽, 약 18분/약 30분). `:42-43`(1부 §8 신설), `:124-125`(2부 §15 신설), `:111`(§11에 정의 낭독 추가), 압축 경로(−9:50)와 예상 Q&A 14번.
- 각주 관례: 신설 장 출처는 "opencode · LLM API 공식 문서 기준 (2026-08-19 확인)" — 실측이 아니라 문서 확인이라 버전 번호 대신 확인일 표기.

## Agent 섹션 (AI 인수인계용)

### 의도 (Intent)

사용자 요청 3건(HTTP 통신 사용자 시점 설명, blindspot/unknown unknowns 개념 설명, 방법론별 결과물 영향 시각화)을 기존 덱 관용구 안에서 최소 삽입으로 구현. 결정의 정본은 requirements/unknowns 문서(각 2회 doc-verifier 통과).

### 제약 (Constraints)

- 새 그림은 인라인 SVG + currentColor 4위계, 그림 앞 visually-hidden 설명, 인쇄/reduced-motion 방어는 기존 규칙 재사용.
- 높이 상한: fragment 장 660px / 무 fragment 장 700px. 실측 413px(1부 8장) · 432px(2부 10장) · 605px(2부 8장).
- HTTP 설명 구간은 "호출자↔사내 LLM"으로 한정 — 방법 5·6의 "사용자↔opencode serve" 구간과 섞지 않음(그림에는 왕복 1개만, 경계 차이는 대본 §8과 Q&A 14).
- 응답 귀환선은 dim 점선 march — 실선 귀환이라는 새 문법을 만들지 않음. march 주기는 1부 값(4,3/-14).
- 해부도 배지는 지도 장 계약("각 장의 배지가 이 지도를 가리킵니다") 준수: 스킬명 + 층 원문자, ①③=accent·④=danger, ② 행 없음.

### 검토한 엣지케이스

| 엣지케이스 | 처리 |
|---|---|
| 요약 상자 정의가 두 줄로 꺾여 700px 초과 | 라벨·문구를 한 줄 용량(한글 36자) 안으로 확정, 605px 실측 통과 |
| 삽입이 auto-animate 모프 체인을 깨는 경우 | 두 삽입 지점 모두 모프 짝과 격리 확인(1부 4↔5장, 2부 2→3→4장만 모프) |
| 대본 [→ n]과 fragment 수 불일치 | 교차 스크립트로 17/17·28/28 전수 대조 통과 |
| SVG 라벨 겹침 | 해부도 동일 행 최소 간격 35.6px 실측 |
| 인쇄 시 새 요소 소실 | SVG 전용 구성 + 각주 `small dim`(인쇄 검정 복구 실측 근거 있음), 새 HTML 클래스 0개 |
| 낭독기에서 SVG 캡션 미낭독 | 신설 2장의 visually-hidden에 캡션 문장 병기 |
| 해시 주소 밀림 | 덱·랜딩 href에 #/N 0건이라 링크 무손상. 문서 참조는 아래 각주 참고 |

### 검증 결과

- check-runner 5종 전부 통과: slides.json 유효(항목 4개), dir↔index.html 4/4, 덱↔대본 대조 PASS, 금지 패턴(plugins:/maximum-scale/외부 리소스) 0건, .nojekyll·slides/sample 유지.
- 헤드리스 실측: 높이 3건 상한 이내, 콘솔 오류 0건, viewBox 넘침 0건.
- 미실시(권장 수동 확인): `?print-pdf` 실제 인쇄 출력 육안 확인, reduced-motion 모드 육안 확인.

### 의도적 범위 제외

- 덱 안의 "여섯 가지"·"다섯" 수량 문구 — 방법·스킬 개수가 변하지 않아 무변경.
- 기존 장의 발표 시간 단축 없음 — 대신 신설 2장을 압축 경로 첫 항목으로 등록.
- 기존 각주 6개의 인쇄 회색 잔존, 숨은 설명문의 인쇄 빈 블록(기존 조건) — 다음 인쇄 개선 사이클로 이월.
- 예상 Q&A는 14번(두 HTTP 경계 구분) 1건만 추가, 기존 13건 무변경.

### 구 보고서 각주

`docs/blindspot/2026-08-20-serve-methods-and-review-report.md:16`의 재현 링크 `#/11(방법 5)`, `#/12(방법 6)`, `#/13(비교표)`와 "16장/27장" 표기는 **이번 삽입 이전 기준**이다. 현재 덱에서는 각각 `#/12`, `#/13`, `#/14`에 해당한다. 발행 문서 동결 원칙에 따라 옛 보고서는 수정하지 않는다.

### 구현 노트 요약

- 이탈 1(기록됨): 해부도 캡션에 ② 부재 사유 한 줄 포함 — unknowns 해소 1의 "대본에서 언급"을 화면+대본 병행으로 확장.
- 이탈 2(적발 후 정정): 해부도 제목 accent·패널 폭 380으로 구현했다가 change-analyzer 적발로 문서 결정값(제목 dim, 폭 390)에 맞춰 정정. 부위 글자 bright는 지도 장 문법이 정본이라 코드 유지, unknowns 해소 12 문구를 정정.
- 보수적 선택: march-border(자율 영역 테두리)는 원리 그림에 미적용. §11 발표 시간 0:45 유지.
- 사용자 확인 필요(체크포인트 질문): 해부도 부위 문구 10칸의 최종 표현 — 아래 게이트에서 확인.
