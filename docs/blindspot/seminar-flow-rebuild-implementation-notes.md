# 팀 세미나 흐름 재구축 (dx-vs-ax + philosophy) 구현 노트

- 시작일: 2026-08-20
- 연관 문서: docs/blindspot/2026-08-19-dx-vs-ax-automation-explainer.md, docs/blindspot/2026-08-19-ai-agent-skills-philosophy-explainer.md, docs/blindspot/2026-08-20-philosophy-harness-viz-explainer.md

<!-- 결정 시점마다 아래 형식으로 append. 사후 재구성 금지. -->

## 2026-08-20 03:00 — 인터뷰 생략, 자율 판단 모드로 진행

- 결정: requirements-interview / blindspot-pass의 대화형 단계를 생략하고, 사용자 지시("질문은 하지 않고 스스로 판단 및 검증")에 따라 모든 결정을 이 노트에 기록하며 진행.
- 이유: 사용자가 명시적으로 무질문 자율 수행을 지시함. 질문 정책 4항(기계적 위임 답변 시 자체 해소 확대)에도 부합.
- 검토한 대안: 인터뷰 강행(사용자 지시 위반), 계획만 제시 후 대기(자율 수행 지시 위반).
- 보수적 선택 여부: 아니오
- 계획과의 이탈: MANDATE 표의 requirements-interview 트리거를 사용자 지시로 대체.
- 사용자 확인 필요: 아니오

## 2026-08-20 03:01 — 두 덱 유지 + 1부/2부 세미나 구조로 재편 (통합 덱 신설 안 함)

- 결정: dx-vs-ax-automation을 1부(LLM 자동화의 효과와 연결 방법), ai-agent-skills-philosophy를 2부(하네스의 효과와 도구 철학)로 유지하고, 덱 간 교차 링크와 표지 파트 표기로 하나의 세미나 흐름을 만든다. 새 통합 덱은 만들지 않는다.
- 이유: 슬러그(URL)는 공개 계약이라 유지가 안전. 두 덱의 주제 분리(자동화 효과 / 하네스 효과)가 사용자가 명시한 두 세미나 목표와 1:1 대응.
- 검토한 대안: 단일 통합 덱 신설(URL 추가·기존 덱 중복 문제), integrated-architecture 재활용(주제가 보안 아키텍처라 부적합).
- 보수적 선택 여부: 예 — 기존 URL 계약 보존을 우선.
- 계획과의 이탈: 없음
- 사용자 확인 필요: 아니오

## 2026-08-20 03:02 — 애니메이션 기술 선택: CSS 키프레임 + reveal fragment/auto-animate만 사용

- 결정: 흐름 애니메이션(파이프라인 이동 점, 대시 행진, 선 그리기)은 CSS 키프레임/트랜지션으로만 구현. SMIL(<animate>)과 JS 애니메이션은 금지.
- 이유: deck-base.css의 prefers-reduced-motion 방어가 CSS 애니메이션만 정지시킴 — SMIL은 정지되지 않아 접근성 방어가 뚫린다. JS 추가는 플러그인 부재 환경에서 유지보수 부담.
- 검토한 대안: SMIL animateMotion(감속 방어 불가), requestAnimationFrame JS(복잡도 증가).
- 보수적 선택 여부: 예 — 표현력 일부(곡선 경로 이동)를 포기하고 접근성 방어를 우선.
- 계획과의 이탈: 없음
- 사용자 확인 필요: 아니오

## 2026-08-20 03:03 — opacity 0에서 시작하는 키프레임의 인쇄 방어

- 결정: 등장 애니메이션은 최종 상태가 opacity 1로 끝나도록 animation-fill-mode: both를 쓰고, @media print에서 animation을 해제해 항상 최종 상태로 인쇄되게 한다.
- 이유: 인쇄 시 애니메이션 중간 프레임(투명 상태)이 찍히면 요소가 종이에서 사라짐 — 기존 cds 덱 흰 블롭 사고와 같은 계열의 위험.
- 검토한 대안: 등장 애니메이션 전면 금지(표현력 손실 과도).
- 보수적 선택 여부: 아니오
- 계획과의 이탈: 없음
- 사용자 확인 필요: 아니오

## 2026-08-20 03:10 — 전체 흐름 재구축 설계 확정

- 결정: 1부(dx-vs-ax) 14장 — 표지 / 여정 지도(신설) / 용어 / DX 파이프라인 / AX 진화(모프 유지) / LLM 자동화 효과: 롱테일 커버리지 차트(신설) / 사다리 / 방법 1~4(추상화 미터 추가) / 비교표 / 브리지 "위임의 대가 — 폭이 벌어진다"(신설) / 끝. 2부(philosophy) 17장 — 표지 / 폭 확산 / 하네스 조임 / 오늘의 지도(신설, 모프 3연속) / 스킬 5장(제약 강도 순: ponytail→grill-me→superpowers→blindspot-flow→fablize) / 자율성 스펙트럼(신설) / 제품 5장(자율성 순: Aider→Copilot WS→LangChain DA→oh-my-opencode→Devin) / 정리(신설) / 끝.
- 이유: 세미나 목표 두 축(자동화의 효과 = 1부, 하네스의 효과 = 2부)에 슬라이드를 1:1 대응. 1부 끝의 "폭이 벌어진다"가 2부 첫 장 "같은 요청, 벌어지는 결과"로 직결되는 단일 서사.
- 검토한 대안: 도구 슬라이드 복잡도순 유지(기존) — 하네스 층 서사와 연결이 끊겨 폐기. 그룹 구분 전용 divider 슬라이드 — 지도/스펙트럼 슬라이드가 겸하므로 불필요.
- 보수적 선택 여부: 아니오
- 계획과의 이탈: 없음
- 사용자 확인 필요: 아니오

## 2026-08-20 03:11 — 시각 언어 통일 규약

- 결정: (a) 실선 상자 = 정적 구성요소, 점선 상자 = 자율 영역(에이전트/샌드박스, 애니메이션 시 대시 행진), accent = 오늘의 초점, danger = 실패·반려 경로, 점선 행진 = 루프. (b) 미터 관용구 공유 — 1부 방법 슬라이드의 추상화 미터(4칸), 2부 제품 슬라이드의 자율성 미터(5칸). (c) 2부 스킬 슬라이드는 하네스 층 배지(①~④)로 지도와 연결.
- 이유: 두 덱을 오가는 청중에게 같은 모양 = 같은 뜻 보장. 세미나 전체의 시각 일관성.
- 검토한 대안: 슬라이드별 자유 표현(일관성 상실), 범례 슬라이드 추가(장 수만 늘어 폐기).
- 보수적 선택 여부: 아니오
- 계획과의 이탈: 없음
- 사용자 확인 필요: 아니오

## 2026-08-20 03:12 — 소소한 자체 해소 3건

- 결정: (a) 2부 도구명 "superpower"를 실제 플러그인 이름 "superpowers"로 정정. (b) 애니메이션 유틸 CSS는 deck-base.css에 넣지 않고 두 덱 인라인 스타일에 중복 기재 — 공유 계약(?v= 캐시, 타 덱 영향) 불변 유지. (c) dataviz 스킬은 로드하지 않음 — 차트 팔레트가 저장소 고정 디자인 시스템(currentColor 단색 + accent)에 이미 구속되어 외부 팔레트 규칙이 적용 불가.
- 이유: (a) 사실 정확성. (b) 두 덱만 쓰는 유틸로 공유 파일 계약을 흔들지 않기 위함. (c) CLAUDE.md 디자인 규약이 스킬 일반론에 우선.
- 검토한 대안: (b) deck-base v2로 승격 — 타 덱 3종 재검증 비용 대비 이득 없음.
- 보수적 선택 여부: 예 — (b)에서 공유 파일 불변을 우선, 중복 ~40줄 감수.
- 계획과의 이탈: 없음
- 사용자 확인 필요: 아니오

## 2026-08-20 03:40 — 검증 루프에서 발견·해결한 사항 (Playwright 3회 반복)

- 결정: (a) 은하 중심부 위 텍스트 가독성 문제를 컨테이너 도형 일괄 어두운 채움 규칙(`svg rect[fill="none"], circle[fill="none"], .panel-fill` → rgba(14,14,14,0.85))으로 해결. 인쇄(흰 종이) 경로에서는 fill 해제. (b) 라벨류(스펙트럼 제품명, 루프 설명, 캡션)는 개별 배경 사각형(stroke 없음) 추가. (c) fablize 말풍선을 화살표 위 중앙으로 이동(게이트 상자에 가려지던 문제). (d) 0.72 불투명도는 은하핵을 못 가려 0.85로 상향.
- 이유: 1280×800 실측 스크린샷에서 은하핵(화면 중앙)과 겹치는 dim 텍스트가 판독 불가 수준으로 확인됨.
- 검토한 대안: 은하 배경 전역 감광(galaxy3d.js 공유 파일 수정 — 타 덱 영향으로 기각), 슬라이드별 수동 배치 회피(중앙 회피 불가능한 구도 다수).
- 보수적 선택 여부: 예 — 공유 파일 불변 유지.
- 계획과의 이탈: 없음 (검증 루프 내 개선)
- 사용자 확인 필요: 아니오
- 검증 결과: 두 덱 31장 전부 넘침 0건, 콘솔 오류 0건(three.js 안내 경고 1건은 vendored 라이브러리 기존 사항), 동작 축소 에뮬레이션에서 애니메이션 정지·인쇄 에뮬레이션에서 채움 해제/검정 복구/장식 점 숨김 확인, 모프 체인(2→3→4) 왕복 내비게이션 정상, 랜딩 카드에 갱신된 설명 표시 확인.

## 2026-08-20 03:55 — 분석 에이전트 중간 단서로 잡은 결함 2건

- 결정: (a) PDF 내보내기(?print-pdf) 경로에서 reveal이 fade-out fragment를 남겨 두어 1부 차트의 ✗(실패)와 ✓(처리)가 같은 좌표에 겹쳐 내보내지는 결함 — `html.reveal-print .fragment.fade-out { display: none }`으로 최종 상태만 내보내게 수정. (b) 2부 덱 SVG 15개에 xmlns 속성 누락(1부는 전부 있음) — 일관성 위해 전부 추가.
- 이유: (a) vendored reveal.js 6.0.1 소스 확인 결과 pdfSeparateFragments:false의 else 분기가 `.fragment:not(.fade-out)`에만 visible을 부여 — fade-out은 기본 표시 상태로 남는다. (b) HTML 인라인에서는 무해하나 파일 간 규약 불일치.
- 검토한 대안: (a) fade-out 기법 자체를 폐기하고 두 슬라이드로 분리 — 교차 전환 표현력 손실로 기각.
- 보수적 선택 여부: 아니오
- 계획과의 이탈: 없음
- 사용자 확인 필요: 아니오

## 2026-08-20 04:05 — 03:03 계획 정정 + 분석 에이전트 지적 4건 반영

- 결정: (a) 03:03에서 계획한 animation-fill-mode 방식은 구현 중 폐기 — 실제로는 흐르는 점의 기본 상태를 opacity 0으로 두고 인쇄에서 display:none으로 제거하는 더 단순한 방식을 썼다(같은 위험을 다른 수단으로 방어). (b) 흑백 인쇄에서 미터의 현재 칸이 색 소실로 구분 불가하던 문제 — 현재 칸을 반전(검정 바탕·흰 글자)으로 표시. (c) 미터의 aria-label이 문단 요소에 걸려 낭독기에서 무시되던 문제 — role="img" 부여로 유효화. (d) 새 SVG 라벨의 font-size 11px 4곳을 12px로 상향. (e) panel-fill 일괄 규칙의 함정(향후 투명 도형이 의도치 않게 채워짐)에 옵트아웃 주석(fill="transparent") 추가.
- 이유: change-analyzer 에이전트의 diff 분석에서 인쇄·접근성 경로의 잔여 결함으로 지적됨. 색만으로 정보를 나르지 않기(WCAG 1.4.1), ARIA 문단 명명 금지 규격 준수.
- 검토한 대안: (b) 미터를 SVG로 재구현(과잉), (c) visually-hidden 텍스트 추가(role="img" 한 속성으로 충분).
- 보수적 선택 여부: 아니오
- 계획과의 이탈: 03:03 항목의 구현 수단 변경 (방어 목표는 동일)
- 사용자 확인 필요: 아니오
- 비고: 이 파일의 03:03 항목이 잇단 삽입으로 파일 끝에 밀려 있던 순서 오류를 이 시점에 시간순으로 복구했다.

## 2026-08-20 04:15 — 랜딩 카드 겹침: A/B 실측으로 기존 한계로 판정, 설명은 압축

- 결정: slides.json의 두 세미나 설명을 압축(64자→37자, 57자→33자 수준)하고, 1024×768에서 남는 카드 겹침(설계 AX 전환 ↔ 1부 카드)은 변경 전(HEAD) 버전에서도 재현되는 기존 랜딩 배치 한계로 판정해 이번 범위에서 제외.
- 이유: HEAD slides.json으로 되돌려 같은 해상도에서 실측한 결과 겹침 11×152px가 이미 존재(축약본 22×145 — 근본 원인은 3D 투영 배치가 카드 높이 충돌을 계산하지 않는 것). 수정하려면 랜딩 styles.css/main.js를 건드려야 하며 이는 세미나 덱 작업과 별개 범위.
- 검토한 대안: .card-desc에 line-clamp 추가(랜딩 CSS ?v= 캐시 범프 필요 — 별도 작업으로 미룸), 설명 원상 복구(정보 손실 대비 이득 없음).
- 보수적 선택 여부: 예 — 랜딩 파일 불변 유지, 겹침 최소화는 설명 압축으로만.
- 계획과의 이탈: 없음
- 사용자 확인 필요: 아니오 (별도 랜딩 개선 작업 제안은 보고서에 기재)
