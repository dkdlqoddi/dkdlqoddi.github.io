# cds-agentic-work 덱 비주얼 개편 구현 노트

- 시작일: 2026-07-27
- 연관 문서: [요구사항](2026-07-27-cds-agentic-work-visual-requirements.md) · [unknowns](2026-07-27-cds-agentic-work-visual-unknowns.md) · [설계](2026-07-27-cds-agentic-work-visual-explainer.md)

<!-- 결정 시점마다 아래 형식으로 append. 사후 재구성 금지. -->

## 2026-07-27 18:50 — 구현 시작, 브랜치 생성

- 결정: `feature/cds-deck-visual` 브랜치에서 작업한다. 대상 파일은 `slides/cds-agentic-work/index.html` 하나다.
- 이유: main은 곧 배포라서 직접 수정 금지(CLAUDE.md). 덱은 자기완결 단일 파일 관례를 유지한다.
- 검토한 대안: 별도 CSS 파일 분리 — 기각 이력이 있어 재기각.
- 보수적 선택 여부: 아니오
- 계획과의 이탈: 없음
- 사용자 확인 필요: 아니오

## 2026-07-27 18:52 — 모핑(3·4·5장) 구현 방식 구체화

- 결정: 세 장의 SVG를 같은 좌표계(viewBox 0 0 900 300)로 그리고, SVG 안 그룹(`<g>`)에 `data-id`를 붙여 모핑한다. CSS로 `transform-box: fill-box`를 지정한다. 실측(플레이라이트 + file://)에서 그룹 모핑이 깨지면 `data-id`를 SVG 루트로만 옮겨 "제자리 교체" 폴백으로 내린다.
- 이유: reveal의 auto-animate는 `data-id`가 있으면 어떤 요소든 짝짓지만, SVG 내부 요소 변형은 브라우저 의존이라 실측 없이는 단정할 수 없다(unknowns 미해소 2번과 같은 계열).
- 검토한 대안: 다이어그램을 HTML 상자로 되돌려 모핑 — 인쇄에서 배치가 무너지는 설계 결정 2의 근거를 정면으로 위반해 기각.
- 보수적 선택 여부: 예 (폴백 경로를 미리 정해 둠)
- 계획과의 이탈: 없음 (설계 구현 규칙 2의 구체화)
- 사용자 확인 필요: 아니오

## 2026-07-27 18:54 — 12장 "빈 그릇 SVG" 단순화

- 결정: 12장은 빈 그릇 그림 없이 큰 숫자 "0"(등록된 업무 자동화 0건)과 정의 문장으로만 구성한다. 단계 수도 2회에서 1회로 줄인다(총합 33 → 32).
- 이유: 큰 숫자 0 자체가 "빈 그릇" 메시지를 전달한다. 그릇 그림은 정보 없는 잉크(장식)라 데이터-잉크 원칙에 어긋난다.
- 검토한 대안: 설계 매핑대로 그릇 SVG 추가 — 시각 정보가 중복되어 기각.
- 보수적 선택 여부: 예 (그리지 않는 쪽을 택함)
- 계획과의 이탈: 있음 — 설계 매핑 표 12장의 시각 유형을 축소. 단계 예산(40 이하)은 준수.
- 사용자 확인 필요: 예 (보고서 체크포인트에서: 12장 단순화를 수용할지, 그릇 그림을 추가할지) → 2026-07-27 19:35 보고서 체크포인트에서 사용자가 "수용"으로 확정.

## 2026-07-27 18:55 — 기존 CSS 도형 클래스 제거

- 결정: `.diagram`/`.box`/`.link`/`.col`/`.row`/`.checks` 클래스와 그 인쇄 되돌림 줄을 삭제한다. 3·5장은 SVG로, 14·19장은 새 `.cards` 격자로 대체되어 사용처가 사라진다.
- 이유: 사용처 없는 규칙은 죽은 코드다(견본 덱 판례). 인쇄 되돌림 목록도 함께 줄어 유지 부담이 준다.
- 검토한 대안: 미래 대비로 남겨 두기 — 죽은 코드 금지 판례에 어긋나 기각.
- 보수적 선택 여부: 아니오
- 계획과의 이탈: 없음 (설계 결정 2의 귀결)
- 사용자 확인 필요: 아니오

## 2026-07-27 19:00 — 5장 화살표 라벨 제거

- 결정: 5장 SVG의 화살표 위 글자("원격 연결", "웹 주소 호출")를 지운다. 화살표가 상자 모서리에 닿던 위치도 안쪽으로 보정했다.
- 이유: 실측 스크린샷에서 라벨이 중개 서버 상자 테두리와 겹쳤다. 같은 정보가 입구 상자 부제와 6장 알약에 이미 있다.
- 검토한 대안: 라벨을 화살표 밖으로 옮기기 — 어느 위치든 상자와 겹치거나 상자 안 빈 공간에 들어가 오독을 만들어 기각.
- 보수적 선택 여부: 예 (정보 삭제가 아니라 중복 제거)
- 계획과의 이탈: 없음 (설계 매핑은 라벨을 명시하지 않았음)
- 사용자 확인 필요: 아니오

## 2026-07-27 19:05 — file:// 실측 결과 (unknowns 미해소 2번 해소)

- 결정: file://에서 해시 이동을 포함한 전체 파이프라인이 정상 동작함을 확인했다. 추가 대응은 하지 않는다.
- 이유: 헤드리스 크롬으로 `file://...index.html#/3`을 열어 4장이 정상 렌더되고 콘솔 오류가 0건임을 실측했다. 우려하던 `history.replaceState` 예외는 발생하지 않았다.
- 검토한 대안: 예외 대비 `hash: false` 강등 — 문제가 실측되지 않아 불필요, 기각.
- 보수적 선택 여부: 아니오
- 계획과의 이탈: 없음 (설계 열린 질문 1번의 예정된 실측)
- 사용자 확인 필요: 아니오

## 2026-07-27 19:10 — 인쇄에서 표지 우주선이 숨지 않는 결함 발견·수정

- 결정: 인쇄 숨김 규칙을 `.deck-stars, .cover-ship { display: none !important; }`로 강화한다.
- 이유: 인쇄 미디어 에뮬레이션 실측에서 우주선이 흰 종이에 흰 얼룩으로 찍혔다. reveal 인쇄 규칙(`.reveal div { display: block }`, 특이성 0-2-2)이 기존 규칙(0-1-0)을 이기고 있었다. 개편 전 덱에도 같은 결함이 잠재해 있었다.
- 검토한 대안: 셀렉터 특이성만 올리기 — reveal 쪽이 요소 나열식이라 !important가 더 단순하고 인쇄 블록의 다른 규칙과 일관되어 기각.
- 보수적 선택 여부: 아니오
- 계획과의 이탈: 없음 (기존 결함의 발견 수정)
- 사용자 확인 필요: 아니오

## 2026-07-27 19:12 — 단계(클릭) 최종 개수 31회

- 결정: 단계 공개 총 31회로 확정한다(설계 예상 33회에서 12장 1회·16장 1회 감소).
- 이유: 12장은 큰 숫자 단순화로 1회가 줄었다. 16장은 스크립트 그림을 처음부터 보여주는 쪽이 제목과 함께 읽혀서 그림 fragment를 뺐다.
- 검토한 대안: 설계 수치 33회 고수 — 설계 문서가 장별 1~2회 조정을 허용하므로 불필요.
- 보수적 선택 여부: 아니오
- 계획과의 이탈: 없음 (허용 범위 내 조정)
- 사용자 확인 필요: 아니오

## 2026-07-27 19:25 — 변경 분석 리뷰 지적 4건 수정

- 결정: (1) SVG 화살표·선 28곳의 회색 하드코딩을 currentColor + dim 클래스로 교체, (2) 모션 최소화 블록에 transition-delay 0 추가, (3) 5장 입구 조각 2개에 자동 페이드 제외 표시(data-auto-animate-unmatched="false") 부착, (4) 13장 필수 칩 3개를 밝은 스타일(done)로 변경.
- 이유: (1) 인쇄 되돌림이 색 속성 하드코딩에는 닿지 않아 흰 종이에서 화살표가 대비 2.7:1 회색으로 남았고 주석("전부 currentColor")과도 불일치했다. (2) reveal이 주입하는 0.2초 시작 지연이 모션 최소화 사용자에게 남았다. (3) 모핑 장의 조각만 0.8초 페이드로 등장해 다른 조각(0.2초)과 리듬이 달랐다. (4) 필수 항목이 진행 표시의 "미도달" 회색과 같아 오독 여지가 있었다.
- 검토한 대안: (1) 인쇄 규칙에 stroke/fill 강제 추가 — 속성별 열거가 늘어나 기각. (4) 칩 전용 새 클래스 — 기존 done 재사용으로 충분해 기각.
- 보수적 선택 여부: 아니오
- 계획과의 이탈: 없음 (설계 결정 3·11의 이행 누락 수정)
- 사용자 확인 필요: 아니오

## 2026-07-27 19:54 — 머지 게이트 통과

- 결정: 사용자가 pre-merge 퀴즈(6문항) 통과를 확인해 main 머지를 진행한다.
- 이유: MANDATE 5항 — 퀴즈 통과 전 머지 금지. 통과 확인이 게이트 해제 조건이다.
- 검토한 대안: 없음 (절차 이행)
- 보수적 선택 여부: 아니오
- 계획과의 이탈: 없음
- 사용자 확인 필요: 아니오 (이 항목이 그 확인의 기록)
