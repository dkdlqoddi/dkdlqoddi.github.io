# Requirements Interview: Slide Order & Content Revisions

말씀해주신 피드백(제목 줄바꿈, LLM 성능 불편함 추가, 애니메이션 슬라이드 순서 변경)을 반영하기 위해, 구현 전 3가지 세부 디자인 결정을 확정하고자 합니다.

### 인터뷰 질문 (구현 방향 확정)

**1. 슬라이드 제목의 괄호 줄바꿈 레이아웃**
Slide 3(Office Network)과 Slide 4(VWP Network)의 괄호 내용(`(Decision & Relay)` 등)을 줄바꿈할 때, 시각적으로 어떻게 표현하는 것이 좋을까요?
- **[A] 단순 줄바꿈 (동일 크기)**: `<h2>` 태그 내에서 `<br>`만 적용하여 두 줄 모두 큼직한 메인 제목 크기로 유지
- **[B] 부제목 분리 (크기 축소, 권장)**: 괄호 내용을 분리하여 메인 제목 아래에 약간 더 작은 글씨(회색빛 부제목)로 배치하여 세련된 계층 구조 형성

**2. LLM 성능 한계에 대한 내용 추가 방식 (Limitations 슬라이드)**
현재 1번 항목이 "환각(Hallucination) 및 잦은 오류"로 되어 있습니다. LLM 성능 한계로 인한 불편함을 어떻게 추가할까요?
- **[A] 4번 항목으로 독립 추가**: 다이어그램 내에 새로운 경고 아이콘(4번)을 만들어 "제한된 LLM 성능으로 인한 업무 효율 저하 및 사용자 불편"을 별도로 강조
- **[B] 기존 1번 항목과 병합**: 기존 1번 경고 아이콘의 텍스트를 수정/확장하여 "낮은 성능과 환각으로 인한 사용자 불편"으로 묶어서 표현

**3. 슬라이드 배치 순서 재구성 확인**
"#/6의 내용(Execution Flow 애니메이션)을 Limitation 바로 앞 페이지로 이동" 요청에 따라, 전체 슬라이드 순서는 아래와 같이 구성되는 것이 맞으실까요?
1) Cover
2) Topology Overview
3) Office Network
4) VWP Network
5) **Execution Flow (애니메이션)** <- *이동됨*
6) Limitations (제약사항)
7) Mitigation (해결책/결론)
- **[A] 네, 위 순서가 맞습니다.**
- **[B] 아니오, (원하시는 다른 순서를 말씀해주세요)**

---

원하시는 옵션(예: "1-B, 2-A, 3-A")을 말씀해 주시면, 바로 Implementation Plan을 수립하여 수정 작업에 착수하겠습니다!
