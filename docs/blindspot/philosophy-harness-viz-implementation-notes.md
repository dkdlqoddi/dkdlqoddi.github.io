# philosophy 덱 리뷰 + 하네스 결과물 폭 시각화 구현 노트

- 시작일: 2026-08-20
- 연관 문서: `2026-08-19-philosophy-harness-viz-requirements.md`, `2026-08-20-philosophy-harness-viz-unknowns.md`, `2026-08-20-philosophy-harness-viz-explainer.md`

<!-- 결정 시점마다 아래 형식으로 append. 사후 재구성 금지. -->

## 2026-08-20 00:55 — 작업 브랜치 생성

- 결정: `philosophy-harness-viz` 브랜치에서 작업 시작.
- 이유: main은 곧 배포라서 직접 커밋 금지 (CLAUDE.md, unknowns 20).
- 검토한 대안: 없음(규칙).
- 보수적 선택 여부: 아니오.
- 계획과의 이탈: 없음.
- 사용자 확인 필요: 아니오.

## 2026-08-20 00:58 — 화살촉 다각형 규격 통일 (열린 질문 해소)

- 결정: 다각형 화살촉을 길이 14, 폭 12(viewBox 단위)로 통일. 촉 끝을 기존 선 끝점에 둔다.
- 이유: 기존 marker 실측(18×12)보다 약간 작고 sample 정본(14×14)과 같은 급. 가장 짧은 화살표(길이 15)도 덮지 않는다.
- 검토한 대안: 기존과 동일한 18×12 — 짧은 화살표에서 선 전체를 덮는 기존 문제가 유지되어 기각.
- 보수적 선택 여부: 예 (짧은 화살표 2곳의 선 길이 연장은 하지 않음 — 모양 불변 원칙).
- 계획과의 이탈: 없음 (explainer 열린 질문 1의 예정된 해소).
- 사용자 확인 필요: 아니오.

## 2026-08-20 00:58 — 기존 슬라이드의 화살표 그룹 구조 유지

- 결정: 기존 `<g stroke=... stroke-width="2">` 그룹 구조를 유지하고, 다각형에만 `stroke="none" fill="currentColor"`를 준다. 정본처럼 선마다 stroke를 내리는 재구조화는 하지 않는다.
- 이유: diff 최소화. 그룹 stroke 상속으로 다각형이 비대해지는 문제는 `stroke="none"`으로 동일하게 차단된다.
- 검토한 대안: 정본 방식(선마다 stroke) — 기존 10장 전면 재구조화라 "모양·배치 불변" 범위를 넘어 기각.
- 보수적 선택 여부: 예 (신규 2장만 정본 구조).
- 계획과의 이탈: 없음.
- 사용자 확인 필요: 아니오.

## 2026-08-20 00:58 — 대각선·곡선 화살촉 좌표는 접선 각도로 수동 계산

- 결정: 대각선 3곳(fablize 반려 곡선, LangChain 상·하 대각선)과 grill-me 곡선 끝은 끝점 접선 방향으로 좌표를 직접 계산해 배치. 일괄 치환 스크립트는 쓰지 않음.
- 이유: patch.py류 스크립트는 과거 사고 원인으로 문서화되어 금지 (unknowns 9).
- 검토한 대안: transform=rotate() 사용 — 동작하지만 저장소 정본이 명시 좌표 방식이라 통일.
- 보수적 선택 여부: 아니오.
- 계획과의 이탈: 없음.
- 사용자 확인 필요: 아니오.

## 2026-08-20 01:15 — 구현 루프 경로의 진행 방향 자체를 반전

- 결정: blindspot-flow의 Implementation Loop 경로를 `M 605 125 … L 425 125`로 뒤집어 그리고, 끝점에 위쪽 화살촉을 달았다.
- 이유: 기존 marker-start 방식은 "경로 시작점에 역방향 촉"이라는 이중 부정 구조라, 경로 자체를 의미(Report에서 Explainer로 복귀) 방향으로 그리는 편이 읽기 쉽다. 렌더 결과 기하는 동일.
- 검토한 대안: 경로는 그대로 두고 시작점에 위쪽 폴리곤만 추가 — 좌표는 같으나 코드 의미가 계속 뒤집혀 있어 기각.
- 보수적 선택 여부: 아니오.
- 계획과의 이탈: 없음 (explainer의 "목적지를 향하게 고친다" 구현 방식 구체화).
- 사용자 확인 필요: 아니오.

## 2026-08-20 01:15 — 열린 경로에 fill="none" 명시 추가

- 결정: 화살표용 열린 path(oh-my-opencode 3곳, Aider 2곳, Devin 2곳)에 `fill="none"`을 명시했다.
- 이유: SVG 기본 fill은 검정이라, L자 경로가 배경(#0e0e0e)과 거의 같은 검정 면으로 몰래 채워지고 있었다. 흰 종이 인쇄에서는 검은 덩어리로 노출될 잠재 결함.
- 검토한 대안: 그대로 두기 — 화면에서는 안 보이지만 인쇄 리스크가 있어 기각.
- 보수적 선택 여부: 아니오.
- 계획과의 이탈: 없음 (결함 수정 범위 내 부수 발견).
- 사용자 확인 필요: 아니오.

## 2026-08-20 01:15 — 수정 블록의 원시 & 문자를 &amp;로 정정

- 결정: 이번에 수정한 블록 안의 원시 `&`(TDD & Review, Report & Quiz, File Edit & Commit)를 HTML 표준 표기로 바꿨다.
- 이유: 원시 &는 브라우저가 관용으로 살려줄 뿐 표준 위반. 손대는 블록만 고치고 다른 곳은 건드리지 않음.
- 검토한 대안: 전체 파일 일괄 정정 — 범위 밖이라 기각.
- 보수적 선택 여부: 예 (수정 블록 밖의 동일 문제는 방치).
- 계획과의 이탈: 없음.
- 사용자 확인 필요: 아니오.

## 2026-08-20 01:35 — [리뷰 발견] 기존 도구 슬라이드 10장 전부 요약 상자 잘림 (수정 보류)

- 결정: 고치지 않고 원상 유지. 리뷰 발견 사항으로만 기록하고 보고서 체크포인트에서 사용자에게 묻는다.
- 이유: 브라우저 실측 결과 도구 슬라이드 10장 모두 내용이 66~125px 넘쳐 요약 상자 마지막 항목이 잘린 채 스크롤로 대피 중(EVA VIEW). main 원본도 동일 수치로 확인되어 이번 변경과 무관한 기존 결함. 시험 삼아 고정 높이 제거 + 글자 0.8em을 적용해 봤으나 실제 콘텐츠 총높이가 760~823px로 드러나 700px 상한을 소폭 조정으로는 못 맞춤 — 즉 유일한 해법은 10장 전면 레이아웃 재작업이고, 이는 사용자가 기각한 "전면 정규화"이자 unknowns 미해소 1·2(다음 사이클)와 같은 작업. 시험 변경은 전부 되돌림.
- 검토한 대안: (a) 요약 글자 축소+높이 자동 — 최악 823px라 불충분, 되돌림. (b) 그림 축소 병행 — 10장 시각 전면 변경이라 범위 초과.
- 보수적 선택 여부: 예 (기존 표시 상태 그대로 유지, 전면 재작업은 다음 사이클로).
- 계획과의 이탈: 없음 (범위 밖 발견의 기록).
- 사용자 확인 필요: 예 — "도구 슬라이드 10장의 요약 상자가 원래부터 잘려 있습니다(66~125px). 다음 사이클에서 레이아웃 재작업으로 고칠까요, 아니면 이번에 범위를 넓혀 고칠까요?"

## 2026-08-20 01:55 — 검사에서 발견된 잔여 리터럴 2건 토큰 치환

- 결정: 인라인 스타일의 `.dim{color:#9aa0a6}` → `var(--dim)`, `.bright{color:#fcfcfb}` → `var(--primary)` 치환.
- 이유: check-runner가 팔레트 리터럴 잔존 1건을 보고. 값은 동일 토큰이 이미 deck-base.css에 있어 치환은 무위험이고, "직접 지정 색 0건" 검사 기준을 문자 그대로 만족시킴.
- 검토한 대안: 그대로 두기 — 45곳 결함(fill/stroke 속성)의 범위 밖이긴 하나, 남기면 검사 기준이 애매해져 기각.
- 보수적 선택 여부: 아니오.
- 계획과의 이탈: 없음.
- 사용자 확인 필요: 아니오.

## 2026-08-20 02:05 — [결함 수정] 신규 B장 반려 화살촉 각도 오류 정정

- 결정: "반려되어 재실행" 곡선의 화살촉 폴리곤을 `230,250 245.2,249.7 240.7,260.9`로 재계산해 교체. 밴드 경계에 걸치던 점 2개(d2, d4)도 안쪽으로 5~7단위 이동.
- 이유: change-analyzer 검산에서 접선 부호 실수 발견 — 끝점 접선 y성분은 250-340=-90인데 +90으로 계산해 촉이 Y거울상으로 44° 어긋나 있었다. 이 곡선은 기존 32개 인벤토리에 없는 신규 33번째 촉이라 검산 목록을 우회했던 것. 점 2개는 낭독기 설명("9개는 띠 안")과 실좌표가 2.5~2.7단위 어긋나 설명 일치성을 위해 이동.
- 검토한 대안: 촉만 고치고 점은 방치 — 그림 설명 문단과 화면 불일치가 남아 기각.
- 보수적 선택 여부: 아니오.
- 계획과의 이탈: 없음.
- 사용자 확인 필요: 아니오.

## 2026-08-20 02:20 — [범위 확장] 요약 상자 잘림, 이번 작업에서 수정 (사용자 승인)

- 결정: 보고서 체크포인트 질문에서 사용자가 "이번에 범위 확장해서 수정"을 선택. 10장 레이아웃 재작업을 진행한다.
- 이유: 사용자 명시 지시. 접근: 고정 높이 500px 제거(내용 기준 높이), 요약 상자 글자 0.85em → 0.7em, 상자 폭 80% → 90%(줄바꿈 감소로 상자 높이 축소). 그림(SVG) 크기와 내용 문구는 건드리지 않는다.
- 검토한 대안: 그림 축소 병행 — 이 덱의 핵심 가치가 다이어그램이라 기각. 문구 축약 — 내용 변경이라 기각.
- 보수적 선택 여부: 아니오 (승인된 확장).
- 계획과의 이탈: 있음 — explainer "의도적 범위 제외" 1번 항목이 범위 안으로 이동. requirements 개정 7로 기록 예정.
- 사용자 확인 필요: 아니오 (이미 확인됨).
