# 세미나 진행 스크립트 작성 구현 노트

- 시작일: 2026-08-20
- 연관 문서: docs/blindspot/2026-08-20-seminar-accuracy-behavior-report.md (직전 사이클)

<!-- 결정 시점마다 아래 형식으로 append. 사후 재구성 금지. -->

## 2026-08-20 06:30 — 형식·위치·구성 결정

- 결정: (a) 위치는 docs/2026-08-seminar-script.md — blindspot 폴더는 프로세스 문서 전용이므로 발표 대본은 docs/ 바로 아래에 둔다(공개 fetch 가능함을 전제로 발표 내용만 담음). (b) 형식은 마크다운 단일 파일 — GitHub 렌더로 읽고 인쇄 가능, 별도 뷰어 불필요. (c) 구성은 준비 체크리스트 / 시간 배분 / 1부 장별 대본(제목·조작 큐·멘트) / 2부 장별 대본 / 압축 경로 / 예상 Q&A(사실 정정 근거 기반). (d) reveal 스피커 노트(<aside class="notes">)는 이 저장소에 플러그인이 없어 동작하지 않으므로 쓰지 않는다 — 별도 문서가 유일한 경로.
- 이유: 발표자는 둘째 화면·종이로 대본을 본다. 조작 큐(→ 시점)는 fragment 수와 정확히 일치해야 하므로 실물 덱에서 기계적으로 추출해 대조한다.
- 검토한 대안: 덱 안에 aside 노트(플러그인 부재로 무동작), HTML 대본 페이지(마크다운 대비 이득 없음), slides/ 하위 배치(덱 폴더는 덱 전용).
- 보수적 선택 여부: 아니오
- 계획과의 이탈: 없음
- 사용자 확인 필요: 아니오

## 2026-08-20 06:31 — 동작 보기 장의 대본 방침

- 결정: 동작 보기 10장은 줄 자체가 대본이므로 멘트를 이중으로 쓰지 않는다 — "한 줄 공개할 때마다 화면 그대로 소리 내어 읽기"를 기본 지침으로 하고, 장마다 도입 한 문장과 마무리 촌평 한 문장만 대본에 적는다.
- 이유: 줄 내용을 대본에 복제하면 덱 수정 시 두 곳이 어긋난다(단일 출처 원칙).
- 검토한 대안: 전 줄 복제(동기화 부채), 줄 무시하고 자유 멘트(추가한 페이지의 목적 훼손).
- 보수적 선택 여부: 예 — 동기화 부채 회피 우선.
- 계획과의 이탈: 없음
- 사용자 확인 필요: 아니오
