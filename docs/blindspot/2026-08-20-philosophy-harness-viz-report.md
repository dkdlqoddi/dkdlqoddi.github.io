# philosophy 덱 리뷰 + 하네스 결과물 폭 시각화 작업 보고서

- 날짜: 2026-08-20
- 기준: main → philosophy-harness-viz 작업 가지 (미커밋 워킹트리)
- 퀴즈: docs/blindspot/quiz/2026-08-20-philosophy-harness-viz.html — 통과 전 머지 금지
- 읽는 법: 코드를 모르는 분은 '요약'과 '스크린샷/데모'까지만 읽으면 됩니다. 그 아래는 개발자와 AI를 위한 상세입니다.

## Human 섹션

### 요약

발표 자료 표지 바로 다음에 개념 설명 슬라이드 2장이 새로 들어갔습니다.
첫 장은 하네스(모델을 감싸는 운영 장치)가 없는 인공지능이 같은 요청에 얼마나 다른 결과를 내는지 보여줍니다. 넓은 부채꼴과 실행 결과 점 12개로 표현했습니다.
다음 장으로 넘기면 부채꼴이 좁은 띠로 부드럽게 조여지고, 4겹의 하네스(지시문, 도구 제한, 작업 절차, 검증 루프)가 나타납니다.
슬라이드의 핵심 문구는 "결과를 하나로 고정하는 게 아니라, 원하는 결과가 더 자주 나오게 폭을 조인다"입니다.
리뷰에서 찾은 결함도 고쳤고 마무리 장을 더해 총 14장이 되었습니다.
추가 발견이 하나 있었습니다: 도구 소개 10장의 요약 상자 마지막 항목이 원래부터 화면 밖으로 잘리고 있었습니다. 확인 질문을 거쳐 이번에 함께 고쳤고, 이제 14장 모두 화면 안에 온전히 들어옵니다.

### 스크린샷 / 데모

로컬 미리보기 서버가 켜져 있습니다: http://localhost:8123/slides/ai-agent-skills-philosophy/
표지에서 오른쪽 화살표 키를 두 번 누르면 새 시각화와 조여지는 애니메이션을 볼 수 있습니다.
브라우저 인쇄 미리보기(Ctrl+P)로 종이 인쇄 결과(검정 도형)도 확인할 수 있습니다.

### 리뷰 포인트 (개발자용)

- `slides/ai-agent-skills-philosophy/index.html:59-96` — 새 슬라이드 A (data-auto-animate, data-id 14쌍: req/band/d1-d12). 밴드 꼭짓점(180,200)이 두 장 bbox의 수직 중심에 놓여야 모핑이 안정 — 부채꼴을 비대칭으로 고치면 꼭짓점이 미끄러짐.
- `index.html:98-160` — 새 슬라이드 B. 반려 곡선 화살촉(:151)은 접선 각도 재계산으로 정정된 좌표(리뷰 중 발견된 Y부호 오류 수정).
- `index.html:308-313` — blindspot-flow 구현 루프: 경로 진행 방향을 반전하고(marker-start 이중부정 제거) 위쪽 폴리곤 촉으로 교체 — 렌더 기하는 동일, 방향 결함만 수정.
- 색 치환 45곳 중 유일한 값 변경: `#c62828` 6곳 → `.danger`(#f05050, deck-base.css:19). 대비 3.43:1 → 5.48:1.
- 화살촉 33개(교체 32 + 신규 1) 전부 인라인 polygon, `stroke="none"` — 그룹 stroke 상속 차단. marker/defs 0건.
- 인쇄 검정 복구는 deck-base.css:196-197의 currentColor 규칙에 의존 — 이번 치환으로 Ctrl+P 경로에서 실측 rgb(0,0,0) 확인 완료.
- 잘림 수정(범위 확장, 요구사항 개정 7): 도구 10장의 flex 컨테이너 고정 `height:500px` 제거(내용 기준 높이), `.summary-box` 0.85em→0.7em·padding 축소, 상자 폭 80%→90%, gap 20→16/margin-top 20→12. 재실측: 14장 전부 scrollHeight ≤ 679(px) — EVA 700 상한 이내, 잘림 0건.

## Agent 섹션 (AI 인수인계용)

### 의도 (Intent)

하네스 비친숙 청중을 위한 결과물 폭 축소 시각화(auto-animate 2장, 도입부)와 리뷰 결함 3종 수정 + 마무리 장. 근거 문서: requirements(개정 6건)·unknowns(해소 23건)·explainer(결정 14건).

### 제약 (Constraints)

- main = 배포. 병합은 퀴즈 통과 후에만.
- 기존 10장은 모양·배치 불변(색/화살촉/설명문만). 전면 정규화는 사용자가 기각.
- 새 2장만 정본 규약(svg.diag, viewBox 900, currentColor 클래스, polygon 촉, visually-hidden 짝).
- 슬라이드 내용 높이 700px 이내(EVA 가드 스크롤 시 center:true bbox 오염 → 모핑 튐).
- 인쇄(Ctrl+P) 검정 복구는 currentColor 체계에서만 동작. ?print-pdf는 어두운 화면 유지(의도).
- 일괄 치환 스크립트 금지(과거 사고 기록) — 전부 수동 편집.

### 검토한 엣지케이스

| 엣지케이스 | 처리 |
|---|---|
| 동작 축소(prefers-reduced-motion) | deck-base.css:189가 전이 0.01ms로 스냅. B장은 fragment 없이 정지 완결 — 에뮬레이션으로 4층+점 12개 존재 확인 |
| 인쇄 두 경로 | Ctrl+P: svg text 계산색 rgb(0,0,0) 실측. 섹션당 1쪽이라 A/B 각각 온전히 인쇄. ?print-pdf: 어두운 화면 유지(기존 계약) |
| 조망 모드(O) | reveal이 모핑 판정을 건너뜀 — 정적 썸네일 2장. 두 장 제목을 다르게 지어 구분 가능 |
| 모핑 실동작 | 전환 중 data-auto-animate="running" 및 B장 짝 14개 매칭 실측 |
| 새 장 높이 | 실측 A 541px / B 679px — 700px 이내 |
| 글자 왜곡 | 크기가 변하는 요소는 글자 없는 밴드뿐. 글자 상자(req)는 위치·크기 동일, 점은 이동만 |
| 짧은 화살표(길이 15) | 촉 14×12로 통일해 선을 덮지 않음 |
| 대각선 촉 4건 | 접선 각도 수동 계산. 신규 1건의 Y부호 오류는 change-analyzer 검산에서 발견 후 정정 |
| 열린 path 기본 검정 fill | fill="none" 명시(7곳) — 인쇄에서 검은 덩어리로 노출될 잠재 결함 제거 |

### 검증 결과

check-runner 13항목 중 12 통과, 유일 실패였던 인라인 `.dim` 리터럴은 var(--dim) 치환으로 해소(재확인 grep 0건). 상세: url(#arrow) 0 / marker 0 / hex fill·stroke 0 / svg 12 = visually-hidden 12 / section 14 / slides.json 정상 / .nojekyll 존재 / viewport 안전 / HTML 파서 정합 / diff는 덱 파일 단독(+258/−106) + 신규 문서 4건. 브라우저 실측: 모핑 발동, 인쇄 검정 복구, 동작 축소 완결성, 새 장 높이 모두 통과. 잘림 수정 후 재실측: 14장 전부 내용 높이 175~679px로 상한(700px) 이내, 잘림 0건.

### 의도적 범위 제외

- 그림(SVG) 내부 글자 크기 상향(실효 10.5px — 기준 24px의 절반), 기존 10장 좌표계 900 통일 — 다음 사이클. (요약 상자 잘림은 당초 보류였으나 병합 전 확인에서 사용자가 범위 확장을 선택해 이번에 수정 — 개정 7.)
- 과거 문서·퀴즈의 옛 장 번호 — 동결.
- 장별 id 부여(딥링크 안정화) — 사용자가 번호 밀림 수용을 선택.

### 구현 노트 요약

결정 10건 기록. 핵심: 화살촉 규격 14×12 통일(열린 질문 해소) / 기존 그룹 구조 유지(diff 최소화, 정본식 재구조화 기각) / 구현 루프 경로 방향 반전 / 열린 path fill="none" 보강 / 잔여 리터럴 토큰 치환 / 신규 촉 각도 오류 정정(analyzer 검산) / 요약 상자 잘림: 보류 → 사용자 승인으로 범위 확장 수정(개정 7). 사용자 확인 필요 잔여 항목: 없음.
